# Checklist verify — RTL đủ đặc tả, đủ tối ưu, đủ đóng góp

Cập nhật 01-09-2026. **Quy tắc của tài liệu này: mỗi dòng phải có một lệnh sinh lại được con
số, hoặc nó là "chưa có số".** Không ô nào được đánh đạt bằng lập luận.

Ba cột: **Đặc tả** (RTL làm đúng thứ P4 mô tả), **Tối ưu** (không tốn hơn mức cần), **Đóng góp**
(số này có phải điều đối thủ không có).

---

## A. Ngưỡng chốt — nguồn chân lý là ADR-0002

| Ngưỡng | Yêu cầu | Số hiện tại | Nguồn | Trạng thái |
|---|---|---|---|---|
| **N1** | ≤ 2,0 điểm | **hai lần nhìn, cạnh nhau (ADR-0008 §2.2)** — lần 1: đạt **2/4** (CNN +0,84 · ResNet −3,31; MobileNet +2,68 ✗ · Inception +2,87 ✗). Lần 2: đạt **3/4** (CNN +0,25 · ResNet −4,01 · MobileNet +2,68 ✗ · Inception **+0,68 ✓**) | `make ds2-all` | ⚠️ trượt **1/4** theo lần nhìn 2, là con số hiện hành (`accuracy-recipe.csv`, commit `afb9af9`). Chỉ thiết lập lượng tử hoá đổi. **Cả hai lần đều trượt MobileNet** — đúng họ mang phần cứng riêng duy nhất đo được (DWCONV, 85 LUT). Ghi chú cũ "trượt 2/4, số 3/4 là SAI" dẫn bảng CŨ (`KET-QUA-DS2-BON-HO.md`, commit `2ccd2b5`) |
| **N1a** | ≤ 1,5 điểm | đạt 3/4 | như trên | ⚠️ một phần |
| **N1b** | ≤ 0,5 điểm | lần 1 đạt 1/4, lần 2 đạt **2/4** | như trên | ⚠️ một phần |
| **N2** | bit-exact RTL vs vector vàng | **1000/1000 nhịp, cả 4 họ** (250 nhịp/họ) trên nhịp DS2 thật | `make sim-coproc` | ✅ đối chiếu `run_blob`, khớp từng byte trên mọi địa chỉ bộ đệm. Lấy mẫu **phân tầng theo lớp** (60/61/61/61/7 — *cả 7* nhịp lớp Q của DS2), và các nhịp chạy **liên tiếp không reset** nên kiểm cả bất biến ADR-0013 **giữa các nhịp** |
| **N3** | T_switch ≤ 1.000 chu kỳ | **98** (bơm 0) / 610 (xoá) | `make isa` | ⚠️ mô hình chu kỳ, chưa đo trên board |
| **N4** | phương sai T_switch ≤ 5 % | chưa có số | cần board | ❌ |
| **N5** | T_switch nguội có số | chưa có số | cần board | ❌ |
| **N6** | ≥ 4 mô hình thường trú | 4 bộ descriptor/thang/bias thường trú, trọng số nạp khi chuyển | `make n7` | ⚠️ ADR-0015 đổi chính sách; chưa xác nhận bằng tổng hợp |
| **N7** | ≤ 24 kB | **13.228 B = 12,92 kB** | `make n7` | ✅ đo từ blob; xem ADR-0015 (bảng cũ 23,77 kB là ước lượng viết tay và đã trượt) |
| **N8** | 0 nhịp bỏ lỡ | T_infer **30.411** / 139.000 (m3 xấu nhất) | `make isa` | ⚠️ mô hình chu kỳ; công thức DWCONV đã sửa (xem C19) |
| **N9** | ≥ 90 % logic dùng chung | **97,1 % đo bằng tổng hợp** = **chi phí cấu hình lại 72 ± 10 LUT** (2,9 % thiết kế). Ước lượng gate bằng tay cho 92,6 % | `make n9-synth` (đo) / `make isa` (ước lượng) | ✅ Nên phát biểu bằng **hiệu số** 72 ± 10 LUT chứ không chỉ bằng tỉ lệ: mẫu số 2.457 làm tỉ lệ nghe chắc hơn thực tế, còn hiệu số cho thấy nó chỉ cách sàn nhiễu ±10 LUT bảy lần. Trong bốn tính năng chỉ **DWCONV (+85 LUT)** là logic riêng đo được; MAXPOOL, ADD, per-channel đều **trong nhiễu** |
| **N10** | độ trễ ba đường | chưa có số | cần board | ❌ |

**Ba ngưỡng chưa có số nào** (N4, N5, N10 — cả ba cần board) và **năm ngưỡng chỉ có ước lượng**. Đây là trạng
thái thật của đề tài, không phải trạng thái mong muốn.

---

## B. Đặc tả — RTL làm đúng thứ P4 mô tả

| # | Hạng mục | Cách kiểm | Trạng thái |
|---|---|---|---|
| B1 | `ecg_pkg` là nguồn chân lý duy nhất của opcode | `tools/compile_model.py` đối chiếu lúc nạp module | ✅ |
| B2 | 12 opcode, mỗi mã có lớp dùng tới | `make isa` → `opcode-usage.csv` | ✅ 12/12 |
| B3 | Requant khớp đường số nguyên **đúng từng bit** | `make sim-requant` | ✅ 23.196 vector |
| B4 | Bias ở miền đầu ra, 8 bit (ADR-0014 §2.3) | như B3 | ✅ |
| B5 | Thứ tự nhân → dịch → cộng bias → kẹp | như B3 | ✅ |
| B6 | ReLU kẹp **sau** khi cộng bias | như B3 | ✅ |
| B7 | Mảng MAC 8 làn, tích luỹ không tràn | `make sim-mac8` | ✅ 4.050 chuỗi |
| B8 | Chuỗi chỉ số lồng khớp `numpy` | `make sim-addrgen` | ✅ 89.668 lần phát, **34 lớp thật** |
| B9 | Bơm 0 ở biên (ADR-0013) | như B8 | ✅ 10.839 lần đọc biên |
| B10 | Bất biến ghi-trước-đọc | assertion trong `ecg_actbuf` | ✅ trong phạm vi testbench |
| B11 | Bộ đệm: 13 bộ đệm logic, byte dùng chung | `make sim-actbuf` | ✅ bộ đệm 3 và 4 trùng byte có chủ ý |
| B12 | **Một cổng đọc, cấp phép mọi nhịp** | như B11 | ✅ 2.000/2.000 nhịp; phân xử đã bỏ vì không còn ai cần đọc song song (C20) |
| B13 | **`CONCAT` theo `dst_off`** (ADR-0014 §2.1) | `make isa` | ✅ cả bốn họ bit-exact; m4 đỉnh 4.416 B vừa 5.120 B (ADR-0015) |
| B14 | Bộ nhớ trọng số + DMA `LOADW` không chặn | `make sim-wmem` | ✅ 4.656 B nạp trong 582 chu kỳ hữu ích = đúng 8 B/chu kỳ; 4.042 phép đọc lệch hàng đúng từng bit |
| B21 | **Bố cục trọng số đúng như bộ sinh địa chỉ đọc** | `make wlayout` | ✅ 2.577 phép đọc 8 byte trên 34 lớp; `make isa` KHÔNG bắt được lỗi này nên phải kiểm riêng |
| B22 | **Blob tự đủ cho bộ tiêu thụ không có mô hình** | `make selfcheck` | ✅ 44 bản ghi / 102 phép kiểm; dựng lại chuỗi hình dạng từ `k`/`stride`/`pad` nên bắt cả `len_out` SAI, không chỉ bằng 0 |
| B23 | **Ghép cả bộ đồng xử lý** (`ecg_coproc`) | `make sim-coproc` | ✅ **N2**: 1000/1000 nhịp (250 nhịp DS2 × 4 họ, phân tầng theo lớp) đúng từng bit so với `run_blob`. Chu kỳ/nhịp đo được: m1 16.084 · m2 32.826 · m3 33.576 · m4 36.562 (mô hình `cycle_model.py` cho 12.383/28.807/30.411/30.255 — đo cao hơn 10–30 % vì mô hình bỏ phần nạp descriptor và xả ống mỗi lớp; đều dưới ngân sách 139.000) |
| B15 | Giải mã descriptor | `make sim-desc` | ✅ 44 lớp thật khớp từng bit trên cả 20 trường + 14 thẻ phi pháp; 3 đột biến đều bị bắt |
| B16 | Sequencer FSM, đường MAC | `make sim-seq` | ✅ 68 lớp đúng từng bit (CONV1D/DWCONV/PWCONV/FC); MAXPOOL/GAP/ADD còn lại |
| B17 | Shim CV-X-IF, chỉ `store.barrier` chặn | chưa viết | ❌ |
| B18 | `MAXPOOL` (bộ so sánh) | `make sim-vecop` | ✅ đúng từng bit, 3 đột biến đều bị bắt |
| B19 | `GAP` (tích luỹ + chia gộp vào requant) | `make sim-vecop` | ✅ đúng từng bit, 3 đột biến đều bị bắt |
| B20 | `ADD` (dùng lại cây cộng psum) | `make sim-vecop` | ✅ đúng từng bit, 3 đột biến đều bị bắt |

**12/20 đạt.** Tám hạng mục còn lại là phần điều khiển, và B13 chặn m4.

---

## B′. Kiểm chứng hình thức — chứng minh, không phải lấy mẫu

`make formal` (SymbiYosys 0.68, `mode prove` = k-induction). Khác testbench ở chỗ
quyết định: một bộ vector cho biết thuộc tính **đúng trên những điểm đã thử**, còn
k-induction chứng minh nó đúng với **mọi** đầu vào và mọi độ dài. Với `ecg_requant`
không gian đầu vào là 2⁴⁹ (psum 24 + mult 11 + shift 5 + bias 9) — đó là khác biệt
giữa "chưa thấy sai" và "không thể sai".

| Module | Thuộc tính đã chứng minh |
|---|---|
| `ecg_requant` | đầu ra luôn trong [−127, 127] · ReLU không bao giờ ra số âm · tầng không tự sinh/mất kết quả |
| `ecg_vecop` | đầu ra int8 luôn trong miền đối xứng (quan trọng cho ADD: hai int8 cộng lại cần 9 bit) · MAXPOOL không bao giờ trả về nhỏ hơn mẫu vừa vào (bắt lỗi khởi tạo cửa sổ bằng 0) · GAP không kẹp psum |
| `ecg_cvxif` | FIFO không bao giờ tràn · **chỉ** rào chắn chặn đường issue · `accept` đúng bằng không-gian-custom-0-và-opcode-hợp-lệ · không quá một lớp chạy cùng lúc |
| `ecg_addrgen` | ba bộ đếm không bao giờ vượt biên · `oob_o` **đúng bằng** "chỉ số ngoài biên" (ADR-0013) · khi không ngoài biên thì chỉ số nằm trong [0, len_in) · `acc_clear` và `out_valid` chỉ cùng bật khi chuỗi tích luỹ dài 1 |
| `ecg_desc` | **20 trường ghép lại đúng bằng từ 128 bit** — phép giải mã không chồng lấn và không mất bit, đúng lớp lỗi đã xảy ra khi trường dự địa đi từ 14 xuống 13 bit ở một bên · `legal_o` không bật khi opcode vượt bảng · nhiều nhất một trong bốn đường định tuyến được chọn |
| `ecg_actbuf` | một phép đọc **bơm 0 không bao giờ chạm bộ nhớ** (nửa mức-module của ADR-0013) · đọc bơm 0 trả về đúng 0 · `rdata_valid_o` đúng bằng yêu cầu trễ một nhịp · **mọi yêu cầu được cấp phép ngay** (điều cả hai sequencer dựa vào để phát một phép đọc mỗi chu kỳ) · địa chỉ đọc luôn trong bộ nhớ |
| `ecg_wmem` *(BMC độ sâu 24, **không** phải quy nạp)* | `rd_valid_o` đúng bằng yêu cầu trễ một nhịp · **phép chiếu theo tính chẵn lẻ đúng chiều** (từ `w0` ở băng chẵn khi `w0` chẵn) · **bộ dịch phễu lấy đúng tám byte từ byte được yêu cầu** · DMA chỉ ghi khi đang chạy và địa chỉ ghi luôn trong băng |

`ecg_wmem` là module duy nhất chỉ có **BMC**, không có k-induction: `abc pdr` sụp
(AssertionError) trên hai băng 292 × 64 bit = 37 k bit trạng thái. Tôi đã thử trừu
tượng hoá bộ nhớ bằng một stub đầu ra tự do (`anyseq`) và **kết quả vô nghĩa**: một
giá trị `anyseq` là giá trị tự do **mới ở mỗi lần tham chiếu**, nên `rd_odd` trong
assertion không phải cùng giá trị với `rd_odd` bên trong `pair`. Dấu hiệu nhận ra:
đổi một biểu thức làm một assertion *khác* bắn, tức báo cáo phụ thuộc thứ tự chứ
không chỉ ra lỗi thật. Phép trừu tượng hoá đó không hợp lệ cho bất kỳ thuộc tính
nào so **hai** biểu thức cùng dẫn từ bộ nhớ — một bài học về thiết lập formal, và
lý do dòng này nói "BMC" thay vì "chứng minh".

Các **giả thiết** trong mỗi khối `ifdef FORMAL` không phải để làm dễ đề bài: chúng
đúng là những điều `ecg_desc` đã kiểm trên descriptor thật (`legal_o`) và `ecg_seq`
chốt lại trước khi phát. Một phản ví dụ vi phạm chúng không nói gì về phần cứng.

Ba assertion đầu tiên tôi viết **sai, không phải RTL sai**, và formal chỉ ra:
`!(cp_start_o && cp_busy_i)` bắn ở bước 4 vì `start` cao trong khi `busy` *vừa* lên
là hành vi đúng — bộ đồng xử lý nâng `busy` để **trả lời** `start`; và hai lần thiếu
ràng buộc trạng thái ban đầu (formal khởi động từ trạng thái thanh ghi bất kỳ) cùng
một lần thiếu `dw_i` trong tập đầu vào được chốt.

**Chưa chứng minh, và nói rõ:** phần còn lại của bất biến ADR-0013 — *byte trong
biên đó đã được một lớp trước ghi* — cần cả `ecg_actbuf` trong phạm vi chứng minh
và vẫn thuộc P6. Ở mức module, thứ chứng minh được là chỉ số nằm trong biên.

---

## C. Tối ưu — không tốn hơn mức cần, đo bằng số

| # | Hạng mục | Trước | Sau | Cách kiểm |
|---|---|---|---|---|
| C1 | Đường psum 32 → **24 bit** | 8.042 cell | **7.099** (−11,7 %) | `make area` *(số đo trước khi sửa ánh xạ mảng PE; `ecg_mac8` nay 4.923 cell)* |
| C2 | Requant: bộ nhân 32×11 → 24×11 | 3.058 cell | **2.419** (−20,9 %) | `make area` |
| C3 | `addrgen`: bỏ DSP48E1 cho `pos × stride` | 1 DSP | **0 DSP** | `make fpga-res` |
| C4 | Bộ nhớ **suy ra được BRAM** | rủi ro | **1 RAMB36E1** | `make fpga-res` |
| C5 | Bias miền đầu ra thay miền psum | bộ cộng **17 bit** trước phép dịch | bộ cộng **9 bit** sau phép dịch; bảng 1.195 → 1.124 B | `make n7`, ADR-0014 §2.3 |
| C6 | Cấp phát byte bộ đệm theo thời gian sống | 5.056 B (vỡ) | **4.419 B** (m4, kể cả hai cổng vào) | `make isa` |
| C7 | Một cổng đọc mux thời gian, giữ N9 | — | không thêm cổng RAM | thiết kế + C10 |
| C8 | 8 PE, không 16 | — | hiệu suất mảng 79 % | roofline P4.4 |
| C9 | **Tổng hợp cả bộ đồng xử lý** | chưa có | ✅ **2.457 LUT / 1.412 FF / 256 CARRY4 / 10 DSP / 3 RAMB18 + 2 RAMB36 = 4 BRAM tile** (trên 140 tile của XC7Z020). Con số cũ "5 RAMB" là **cộng hai đơn vị khác nhau** — RAMB18 với RAMB36 — do awk dùng mẫu `/RAMB/` gộp cả hai; và nó nằm ngay cạnh "140 RAMB36" trong cùng bảng. Một BRAM tile chứa **một** RAMB36 *hoặc* **hai** RAMB18, nên chi phí thật là `RAMB36 + ceil(RAMB18/2)`. Quy ra tile thì yosys và Vivado **thoả thuận hoàn toàn** (phiên Vivado đo độc lập): `ecg_coproc` 4 tile = 4 RAMB36 của Vivado, `ecg_actbuf` 2 tile = 2 RAMB36, `ecg_wmem` 2 tile = 2 RAMB36. Khác biệt chỉ là **độ hạt**: yosys cắt 6144×8 thành 3 RAMB18 (2048×9), Vivado ghép thành 2 RAMB36 (4096×9) — cả hai đều đúng. LUT 2.455 → **2.457** khớp §C10 | `make fpga-res` |
| C22 | **Thuộc tính formal CHỨC NĂNG đầu tiên** — chứng minh 24 bit psum là *đủ*, không chỉ "chưa thấy tràn" | 27 thuộc tính cũ đều là bất biến cấu trúc | ✅ k-induction pass. Bất biến quy nạp là dạng tỉ lệ \|acc\| ≤ 127² × số số hạng; chỉ \|acc\| ≤ PSUM_MAX thì **không** quy nạp được | `make formal` (`mac8.sby`) |
| C23 | **Thử đột biến cho C22** — cho phép 211 số hạng, giữ chặn 210 | một thuộc tính "pass" không tự chứng minh rằng nó không rỗng | ✅ `prove` induction **FAIL** đúng dòng cần → chặn 210 là chặt. Kèm một kết luận nữa: `bmc` của bản đột biến **PASS** (phản ví dụ cần 211 bước, sâu hơn depth 24) → **BMC một mình vô dụng cho thuộc tính này** | `make formal-mutant` |
| C24 | **Ước lượng ba mảnh CV-X-IF còn thiếu** (dualwrite, ngoại lệ, `commit_kill`) bằng phép ĐO | 39 LUT là sàn dưới, phần thiếu "chưa được ước lượng" | ✅ **39 → 56 LUT, 52 → 96 FF** (+0,7 % hệ thống). `dualwrite` = 0 LUT (dây nối đất, mọi lệnh ghi ≤ 1 rd); `commit_kill` giải bằng **hoãn tới commit** thay vì lùi con trỏ FIFO. Bản phác nằm ngoài `40-rtl/src`, chưa có testbench → là ước lượng *diện tích*, không phải tuyên bố *đúng* | `make est-cvxif` |
| C26 | **Đo phương án bias rẻ hơn (E3-9)** — kẹp bias về int8 thay vì bảng 9 bit | 562 B chi cho **đúng 1 mục** trong 562 vượt int8 (−170, `m3.blocks_1_dw_0` kênh 3) | ✅ **Kẹp mục đó làm m3 mất 9,34 điểm macro-F1** → 562 B là chính đáng, ADR-0014 §2.3 đứng. Phép kiểm nội tại: m1/m2/m4 lệch đúng **0,0000** với 0 mục bị kẹp, nên 9,34 điểm là do một mục bias chứ không do lỗi bộ đo. Lý do một kênh đắt thế: lớp đó là **DWCONV**, không có phép trộn kênh nào để pha loãng — lời phê ban đầu áp trực giác conv thường vào lớp depthwise. Đo trên validation, seed 0, bằng `run_blob` (đường phần cứng) | `make bias-clamp` |
| C25 | **Quyết descriptor 16 B hay 24 B** kèm số | biên bằng 0 sau sáu lần thiếu trường | ✅ Đo cả ba mặt: LUT giải mã **0** (15 LUT cả hai), N7 **+384 B** (3,4 % phần dư), T_switch **667 → 680** (còn 32 % biên). **Quyết: giữ 16 B qua P6, nới 24 B khi đóng băng cho ASIC** — mốc là thời điểm đóng băng, không phải chi phí: hôm nay hết biên tốn một lần `make isa`, sau tape-out là bất khả | `make est-cvxif`, `cycle_model.py` |
| C10 | **N9 đo bằng báo cáo tổng hợp** | ước lượng gate 92,6 % | ✅ **97,1 %** (nền 2.385 / cả thiết kế 2.457 LUT) | `make n9-synth` |
| C11 | Đường tới hạn, tần số đạt được | chưa có | ✅ **cả hai đường đã chạy.** **ASIC (OpenSTA, sky130):** `ecg_coproc` toàn chip **29,4 MHz** kẹp 34,0/33,5 ns sau detailed route + SPEF thật; `ecg_seq` một mình **40,7 MHz** kẹp 24,53/24,25. **FPGA (Vivado, `xc7z020clg400-1`):** `ecg_coproc` **62,9 MHz** kẹp 15,909/15,510 sau P&R; lõi CV32E40X **52,9 MHz** kẹp 18,913/18,593. **Đường tới hạn nằm trong bộ requant, không trong mảng MAC** — `ecg_requant` 42,3 MHz so với `ecg_mac8` 114,2. ⚠️ Ba cạm bẫy đã gặp ở chính ô này: (a) `T − WNS` **không** cho Fmax (công cụ chỉ tối ưu tới ràng buộc, **và** đường tới hạn của chip là nửa chu kỳ — xem C46); (b) **độ sâu logic không phải độ trễ**: `ecg_cvxif` sâu nhất (42 mức) mà **nhanh nhất** (320,5 MHz); (c) mọi con số phải nêu **giai đoạn** — số sau tổng hợp và số sau P&R không so được với nhau | `make depth`, `asic-freq.csv`, `impl-ecg_coproc-20260902T104845Z.csv`, `core-impl-20260902T092500Z.csv` |
| C12 | Ghép hai phép nhân int8 vào một DSP48E1 | 8 DSP | ❌ chưa làm | `make fpga-res` sau C9 |
| C13 | **Đọc 8 byte lệch hàng trong một chu kỳ** (hai băng theo chẵn lẻ) | 2 chu kỳ → hiệu suất MAC 50 % | **1 chu kỳ**, 285 LUT | `make sim-wmem` |
| C14 | MAXPOOL/GAP/ADD gộp một đường dữ liệu (chung 1 bộ so sánh, 1 bộ cộng) | — | **124 LUT** cho cả ba | `make fpga-res` |
| C15 | CONCAT bỏ hẳn bản ghi descriptor (ADR-0014 §2.1) | 54 bản ghi = 864 B | **48 bản ghi = 768 B** | `make n7` + `make isa` |
| C16 | **Chuyển vị bố cục trọng số sang `[cin, k, cout]`** | **0/34 lớp** đọc được 8 byte liền nhau (bước 3…210 B) | ✅ **34/34 lớp**, 2.577 phép đọc 8 byte đúng | `make wlayout` |
| C17 | Điền `len_in`/`len_out`/`cin`/`cout` cho mọi bản ghi | **26/44 bản ghi** thiếu trường phần cứng cần | ✅ **0/44** thiếu | `make selfcheck` |
| C18 | Xả requant ghép đường ống với nhóm sau | xả nối tiếp | ✅ **+3,8 %** đo trên 68 lớp thử | `make sim-seq` |
| C19 | **DWCONV chạy một kênh ra mỗi lần** | — | +50,4 % chu kỳ MAC của m3, đổi lấy biên N9 | `make sim-seq`, `cycle_model.py` |
| C20 | **Bỏ cổng đọc kích hoạt thứ hai** | N9 **90,1 %**, actbuf 318 LUT | **N9 92,6 %**, actbuf **207 LUT** | `make isa` + `make fpga-res` |
| C21 | Dùng chung một `ecg_requant` cho hai sequencer | 2 bản, 217 LUT + 1 DSP đếm hai lần | ❌ chưa làm | `make fpga-res` sau khi ghép |
| C22 | `pos × stride` bằng dịch, không DSP (như C3) | 2 DSP | **1 DSP**, +36 LUT | `make fpga-res` |
| C23 | Đỉnh mức bộ đệm tính cả hai cổng vào | 4.416 B (thiếu 259 B) | **4.419 B** (hai cổng vào vào đúng bộ cấp phát) | `make isa` |
| C24 | **Dùng trọn 3 RAMB18: 5.120 → 6.144 B** | m4 thất bại cấp phát ở 5.120 B | **cả 4 họ cấp phát được**, vẫn 3 RAMB | `make isa` + `make fpga-res` |
| C25 | `buf_base` vào 13 bit dự địa của descriptor | tính rồi nhưng không đóng gói | ✅ blob tự đủ về nền bộ đệm | `make sim-desc` |
| C26 | `dst_off × len_out` bằng dịch-cộng | dùng `dst_off` trực tiếp (SAI) | ✅ +45/+39 LUT, 0 DSP thêm | `make sim-seq`, `make sim-seq-vec` |
| C27 | **Bias 9 bit thay vì 8** | 1/562 mục cuộn (−170 → +86), cả kênh sai | ✅ bảng 1.124 B, N7 12,92 kB | `make sim-coproc` |
| C28 | Nền bảng bias cộng dồn, không nằm trong descriptor | 128/128 bit đã dùng hết | ✅ cộng dồn `cout`, chính xác vì mọi lớp có nhân đều có bias | `make sim-coproc` |
| C29 | **Diện tích SRAM từ macro thật sky130 + tỉ lệ SRAM/logic** | chưa biết phần nào của chip tốn diện tích | ✅ ⚠️ **BỐN đại lượng gọi là "utilization", và cái OpenROAD in ra dùng mẫu số CORE chứ không phải DIE.** Với lõi CV32E40X: mục tiêu `initialize_floorplan` **50** (trên 165.414,9 µm² trước P&R) · GPL sau floorplan **51,21 %** · `report_design_area` in **65 %** · nhưng `design_area/die = 213.789/340.098,9 = ` **62,86 %**. Lệch 2,1 điểm, quá lớn để là làm tròn: log có `Core area lower left (4.000, 4.000)` — margin 4 µm mỗi cạnh, nên mẫu số của 65 % là **328.906 µm² core**, nhỏ hơn die **3,3 %**. Cùng chuyện với `ecg_coproc`: **52 %** là trên core, còn `2.582.848/5.020.467,6 = ` **51,45 %** trên die. Bài báo từng ghi *"a 5,02 mm² die (52 % utilised)"* — gán tỉ lệ của **core** cho **die**; đã sửa thành *"die whose core is 52 % utilised"*. Một phần trăm mà không nêu **mẫu số** thì chưa phải một con số. **2.462.795 µm² = 2,463 mm² DIỆN TÍCH CELL + MACRO. SRAM : logic = 19,8 : 1. Logic chỉ 4,8 %.** ⚠️ **Đây KHÔNG phải kích thước chip.** `design_area` của OpenROAD là tổng diện tích cell, còn `die` là mảnh silicon: P&R thật cho `ecg_coproc` với ba macro SRAM cho **die 5.020.468 µm² = 5,02 mm² ở 52 % util**, tức **2,04×** con số này. Và bộ số này **thiếu cả CPU**: lõi CV32E40X là 165.415 µm² cell (P34), nên SoC là **2,639 mm² cell+macro** ở **8,0:1** (số hạng logic của SoC là cell **129.218** của bản macro thật + lõi 165.415 = 294.633; dùng cell 118.356 của bản blackbox thì ra 2,628 mm² / 8,3:1 — **hai cặp, không trộn được**), die **≥ 5,02 mm²** — chặn dưới, không phải ước, vì die không tỉ lệ tuyến tính khi thêm một khối lớn vào một floorplan đã bị 6 macro cứng ấn định hình dạng vùng trống. Ba đại lượng phải gọi đúng tên: cell+macro sau tổng hợp **2.462.795** · design area sau P&R **2.582.848** (std cell 118.356 → 147.907) · **die 5.020.468**. Ba instance SRAM thật: `actbuf u_mem` 8 bit × 6144 = 1.141.572 µm²; `wmem u_even` và `wmem u_odd` mỗi bank 64 bit × 292 = 601.433 µm². ⚠️ **Bản đầu ra 2,164 mm² / 17,3:1 và cả hai đều SAI** — hai lỗi độc lập: (a) coi `wmem` là *một* vùng 4.664 B thay vì **hai bank riêng** (thiếu 33 % phần wmem, và lỗi này cho một con số *hợp lý* nên không tự lộ); (b) bỏ qua chia **lane** — một macro 128 bit chứa 16 ô 8 bit mỗi từ (lỗi này *thất bại rõ ràng* nên tự lộ). Script nay đọc `ECG_ACT_BYTES`, `ECG_WMEM_BYTES` và công thức `BWORDS` **từ RTL** và liệt kê instance thật | `make asic-sram` |
| C30 | **Bộ đếm shim 32 → 6 bit** | 117 LUT, 114 mức logic | **39 LUT (−67 %), 42 mức (−63 %)** | `make depth` + `make fpga-res` |
| C31 | **Kiểm chứng hình thức** (SymbiYosys) | chỉ có testbench | ✅ **27 thuộc tính / 7 module**; 22 chứng minh bằng k-induction, 5 của `ecg_wmem` là BMC độ sâu 24 | `make formal` |
| C32 | **Tối ưu thật sự: thu `ecg_actbuf` 6.144 → 4.448 B** | ADR-0015 chọn 6.144 B vì trên FPGA 3 × RAMB18 là *miễn phí* | ⚠️→🔜 **CON SỐ 9,6 % BỊ BÁC BỎ, và chứng cứ đã nằm trong kho từ trước.** `actbuf-allocator.csv` ghi: tối thiểu **THẬT** là **5.251 B** (bản ghi 7 của m4, `inc2_b3_0`, cần **960 byte liên tục**); 5.250 → m4 **cấp phát thất bại** (lỗ bị phân mảnh thành 256/768/125 B); và **4.608 cũng thất bại** — chính dòng đó mang ghi chú *"đề xuất ban đầu — tưởng tiết 237.492 µm² = 9,6 %"*. Mục tiêu 4.448 B của C32 nằm **DƯỚI** một kích thước đã thất bại, nên con số 9,6 % gắn vào một cấu hình **không cấp phát được**. Đỉnh dùng đo được (4.419 B) là một **chặn dưới của nhu cầu**, không phải kích thước khả thi: một bộ cấp phát cần lỗ **liên tục**, không cần tổng byte. **Kích thước khả thi đầu tiên: 5.376 B** → tiết kiệm **768 B**, tức **45 %** của cái C32 hứa. **KHÔNG quy ra µm² bằng phép chia tuyến tính** — SRAM không tỉ lệ tuyến tính theo byte (granularity, decoder), và `tools/asic_sram.py` có mô hình thật; người dùng đang **tạm dừng ASIC** nên phần µm² để lại. Nên đây **không** còn là "hạng mục tối ưu lớn nhất còn lại của cả dự án" cho tới khi có số từ `asic_sram.py` ở 5.376 B | `90-results/tables/actbuf-allocator.csv`; `make asic-sram` khi quay lại ASIC |
| C40 | **Tần số sau global route, quét bằng tìm nhị phân trên chu kỳ** | chưa có số tần số ASIC nào | ✅ `ecg_cvxif` **320,5 MHz** (3,12 ns) · `ecg_mac8` **114,2 MHz** (8,75 ns) · `ecg_seq_vec` **57,4 MHz** (17,40 ns) · `ecg_requant` **42,3 MHz** (23,59 ns) · `ecg_seq` **40,7 MHz** (24,53 ns). util 45 %. **Ba giới hạn phải nêu kèm**: (a) số **sau global route**, không phải post-route — `detailed_route` còn lỗi nên số cuối sẽ *xấu hơn*; (b) **không có PDN** nên met4/met5 trống hơn thực tế và định tuyến dễ hơn; (c) netlist P&R có chèn cell đệm cho net alias | `tools/asic-flow.sh quet-tan-so` |
| C41 | **Đường tới hạn: đo được là reg-to-reg, và SUY LUẬN là đi qua `ecg_requant`** | trực giác: 8 làn MAC + tích luỹ 24 bit là chỗ thắt | ✅ **Đo trực tiếp**: mọi đường xấu nhất là **reg-to-reg trong nhóm `clk`**, tất cả khởi hành từ **cùng một flop** và toả ra ≥ 5 flop đích, slack 0,26–0,40 ns ở chu kỳ 24,53 ns → điểm đóng là thật. ⚠️ **Suy luận, không phải vết đường**: `ecg_requant` đứng riêng có độ trễ tổ hợp **20,93 ns** (`ECG_IO_FRAC=0`) và `ecg_seq` cần **24,53 ns**, nên nón tổ hợp của requant chiếm **~86 %** chu kỳ. Đó là kết luận từ *hai phép đo*, **không** phải từ việc lần theo đường trong báo cáo — vì `read_slang` **làm phẳng phân cấp** trước khi yosys chạy, nên tên instance (`u_rq/...`) **không thể lấy lại** trong luồng này. Đã thử ba cách (bỏ `rename -enumerate`, bỏ `-flatten`, giữ tên) và cả ba đều không đổi được gì. `ecg_mac8` nhanh hơn **2,4×** (8,75 ns), nên nó chắc chắn **không** phải chỗ thắt. **Hạng mục: chia tầng ống bộ nhân requant** (psum 24 bit × mult 11 bit → tích 35 bit) | `asic-freq.csv`, log `pnr-seq-baocao2-*` |
| C41b | **Ngân sách trễ I/O của khối đứng riêng làm lệch so sánh** | so trực tiếp tần số các khối | ⚠️ Mặc định luồng dành **10 % vào + 10 % ra**, tức 20 % chu kỳ bị trừ trước khi logic được dùng — và khối đó *không* đứng riêng trong thiết kế thật. `ecg_requant` 23,59 ns (10 %) → **20,93 ns** (0 %). Bất nhất phát hiện được nhờ điều này: `ecg_seq_vec` (17,40 ns) nhanh hơn `ecg_requant` (20,93 ns) dù **chứa** nó — giải thích là **lan truyền hằng số trong ngữ cảnh** (seq_vec ghim một số đầu vào của requant), một hiệu ứng thật, không phải lỗi đo. Nay `ECG_IO_FRAC` điều khiển được và **mọi con số phải nêu kèm giá trị nó** | `ECG_IO_FRAC=0 tools/asic-flow.sh quet-tan-so` |
| C50 | **Công suất SRAM đo được — và tôi đã ghi SAI rằng nó không có model** | tài liệu Bước 5 ghi "SRAM là blackbox không có model công suất" | ❌→✅ `.lib` của macro OpenRAM **CÓ** nhóm công suất (dòng 8 `leakage_power_unit : "1mW"`, dòng 107 `cell_leakage_power : 0.039892`, dòng 326–331 `internal_power` scalar `8.954866e+01`). **Tổng SRAM 42,5 mW** @ 40,7 MHz (actbuf 22,1 · wmem chẵn 10,2 · wmem lẻ 10,2; rò rỉ 114 µW). Đơn vị **kiểm chéo**: internal là pJ, 4 × 89,54866 pJ × 40,7664 MHz = 1,4602e−2 W khớp OpenSTA — hệ số 4 = hai chân clk × hai sườn | `tools/asic-sram-power.sh` |
| C51 | **Tổng công suất và năng lượng mỗi nhịp** | chưa có chỉ số so được | ✅ @ 40,7 MHz: logic 4,77 + SRAM 42,50 = **47,23 mW**, **SRAM = 89,9 %** (tỉ lệ 8,9:1, ít gay gắt hơn diện tích 19,8:1). @ 13,16 MHz (đúng đủ): **15,28 mW**. **Năng lượng/nhịp = 42,43 µJ ở cả hai điểm** | `asic-power.csv`, `asic-power-sram.csv` |
| C52 | **Ở năng lượng mỗi nhịp, TA THUA** | — | ❌ 42,43 µJ so với Mao (SNN) 0,30 µJ (**thua 141,7×**), Zhang 2023 mô hình nhỏ 2,05 µJ (**thua 20,7×**), mô hình lớn 14,27 µJ (**thua 3,0×**). Nguyên nhân chi phối là **bộ nhớ mở**: 89,5 pJ/sườn cho 4 kB, cùng hiện tượng OpenRAM kém đặc ở C29. Nhưng đó **không phải lời bào chữa hợp lệ** — nếu báo số của luồng PDK mở thì bị so bằng số đó. Cách trung thực: báo cả số đo được và phần nào là tính chất của bộ sinh bộ nhớ | `asic-power-sram.csv` |
| C53 | **42,5 mW là BIÊN TRÊN, vì model không phân biệt được có truy cập hay không** | tính là số thật | ⚠️ `internal_power` chỉ nằm trên hai chân đồng hồ, **không** trên `din`/`addr`/`csb`/`web`, và giá trị **giống nhau cho mọi `when`** kể cả `csb0 & web0` (bỏ chọn hoàn toàn) — đã kiểm bằng cách chạy `csb0=csb1=1`: **con số không đổi**. Nên số lần truy cập mỗi nhịp **không dùng được** để tính chi li hơn; muốn thế phải đặc trưng hoá lại bằng SPICE (**chưa đo**) | đọc `.lib` |
| C54 | **Chặn đồng hồ cổng ghi SRAM — hiện thực, bốn cửa, VÀ phần chia cổng ĐO thay vì giả định** | phép chiếu giả định mỗi cổng chiếm 50 % công suất macro | ✅ Đo bằng cách **không tạo đồng hồ cho cổng ghi** (`ECG_WR_DUTY=0`): 42,46 → **23,5 mW**, nên cổng ghi là **44,76 %** công suất macro, **không phải 50 %**. Với duty đo được (actbuf ghi 0,1227 · wmem ghi 0): cắt **17,77 mW = 41,7 %** SRAM → SRAM **24,69 mW**, tổng **29,46 mW**, năng lượng **26,47 µJ/nhịp**, thua Zhang 2023 mô hình lớn **1,85×**. ❌ **Phép chiếu ở vòng trước ước QUÁ CAO 3,42 mW = 20 %** (nó cho 21,19 mW cắt được, 26,04 mW tổng, 23,40 µJ) — nguyên nhân đúng là giả định 50/50. **Giá diện tích** 6 × `dlclkp_1` = **105,1 µm² = 0,0043 % chip**. Phép đổi: 0,0043 % diện tích cho **37,5 %** tổng công suất | `tools/asic-sram-power-gated.sh`, `make sim-sram-macro` |
| C54b | **Ba dạng cổng chặn, hai dạng đầu SAI — và cách phân biệt** | — | ⚠️ `always_latch if (!clk_i) …` → **55.475 lệch**; `always_ff @(negedge clk_i) …` → **55.475 lệch, y hệt**; `assign clk_o = clk_i & en_i` → **0 lệch**. **Cùng một con số lệch ở hai dạng rất khác nhau** là dấu hiệu nguyên nhân nằm NGOÀI dạng cổng. Nguyên nhân thật: `tick()` của dự án làm `clk=1; eval; clk=0; eval` — **cạnh lên trước** — nên chốt ở nửa chu kỳ thấp lấy mẫu enable của chu kỳ **trước** và cổng lệch một chu kỳ. Mẫu lệch xác nhận: **lần ghi đầu tiên vào mỗi bank bị mất** (địa chỉ 0 và 4096). AND trực tiếp đúng vì `en_i` là **đầu ra thanh ghi** nên ổn định suốt chu kỳ; trong silicon tổng hợp ánh xạ nó thành `dlclkp_1` và chốt bên trong cell lo phần chống xung giả. **Đây là đúng lỗi mà chú thích của chính module đã cảnh báo** — nay chú thích ghi cả ba dạng kèm con số lệch, vì một cảnh báo không có con số thì không ngăn được ai | `40-rtl/asic/ecg_sram_macro.sv` |
| C57 | **Verilator dùng chung MỘT ký hiệu VCD cho các tín hiệu giống nhau** | bảng khoá-theo-ký-hiệu | ⚠️→✅ `a_req_o` và `w_req_o` được lái **giống nhau** trong `ecg_seq` nên Verilator gộp chúng vào một ký hiệu, và bảng ký_hiệu→tên của tôi **ghi đè mất một tên** — script báo "không thấy `a_req_o`" trong khi nó có trong tệp. Sửa sang khoá-theo-tên, cho phép nhiều tên chung một ký hiệu. Duty trùng nhau (0,9383) không phải trùng hợp mà là **cùng một dây** | `tools/vcd_port_duty.py` |
| C66 | **P16 đóng: đỉnh bộ đệm KHÔNG giảm được xuống ≤ 4.096 ô bằng cách sửa bộ cấp phát** | tưởng có 512–768 B dư của bộ cấp phát | ❌ Phần thưởng sẽ rất lớn nếu được: đỉnh ≤ 4.096 ô cho phép **một** macro `128x256` thay vì `128x256`+`64x256`, tiết kiệm **419.463 µm² = 17 % chip**. Nhưng đo được thì không có phần dư nào. Đỉnh của m4 là **4.419 B vùng sống THẬT** tại lớp 7: `{RR 3, rec1 1920, rec3 768, rec5 768, rec7 960}`. Đường duy nhất còn lại là đổi kiến trúc m4 (tensor lớn nhất là cat của inc1, 1.920 B = 30 kênh × 64 vị trí, sống từ lớp 1 tới lớp 7), và điều đó đòi huấn luyện lại cùng **một lần nhìn DS2 thứ ba** → **chặn bởi ADR-0008**, cùng lý do với P14 | `make isa` |
| C67 | **Một giả thuyết của tôi bị chính phép đo bác bỏ — và phép thử trong bộ nhớ đã cứu một thay đổi rủi ro** | — | ⚠️ Tôi phát hiện `allocate_buffers` **lẫn chỉ số bản ghi với id bộ đệm**: `last_use` khoá theo chỉ số bản ghi (dòng 630) nhưng được gán bằng `last_use[v] = i` với `v` là **id bộ đệm** (dòng 638); ở m4 hai thứ trùng số, nên tôi kết luận bản ghi 3 (bộ đệm 2, 768 B) bị giữ sống tới lớp 8 thay vì lớp 4. Lập luận nghe chắc và phần thưởng là 17 % diện tích. **Đã thử sửa TRONG BỘ NHỚ trước khi sửa tệp**: đỉnh **y nguyên** 4.419 ở cả bốn họ, `buf_base_valid = 1`. Giả thuyết sai. Một lần chạy đã tránh được một thay đổi rủi ro vào hàm quyết định bố cục blob — đúng hàm mà C39 cho thấy thất bại im lặng của nó sinh ra blob sai. Sự lẫn chỉ số **vẫn là mùi mã** và nên dọn, nhưng nó không phải đòn bẩy diện tích | đọc mã + thử trong bộ nhớ |
| C68 | **Một lệnh CÓ TRONG ĐẶC TẢ bị thi hành thành một lệnh KHÁC** — `ecg_cvxif` giải mã `STORE` với `fn3=000` (biến thể ghi DMA, `isa.md §3.3`) thành **một lớp tính toán** | đi tìm một lỗ độ phủ, không đi tìm lỗi này | ✅ **Nguyên nhân:** `is_compute` được định nghĩa bằng **phần bù** (`in_space && !is_barrier && !is_loadw`), nên mọi cặp `(op, fn3)` chưa định nghĩa rơi vào nó. Ba cặp rơi sai chỗ: op 0..9 với `fn3 != 000`, `LOADW` với `fn3 != 001`, và `STORE` với `fn3 = 000`. **Đặc tả `isa.md §2` nói rõ `(ECG_OP, fn3)` là MỘT mã lệnh** — *"cùng một `ECG_OP` ở hai lớp là hai lệnh khác nhau"* — nên một cặp chưa định nghĩa phải bị **TỪ CHỐI** để lõi bẫy được. **ĐO chứ không suy:** một vector ba lệnh cho `1 lop phat / 0 ky vong`; sau khi sửa cho `1 tu choi / 0 lop phat`. **Định nghĩa đặt MỘT chỗ** (`ecg_pkg::ecg_enc_dinh_nghia`) để RTL và formal dùng chung một nguồn. ⚠️ **DẠNG của biểu thức đắt hơn CHỨC NĂNG của nó:** bản ba nhánh `if` tốn **0,2045 ns** trên chu kỳ 3,12 ns (slack +0,2051 → +0,0006), tức mạch giải mã NẰM TRÊN đường tới hạn; bản một so sánh cho **+0,2549**. Tương đương **chứng minh vét cạn** 96 cặp (0 lệch, 12 cặp được nhận — một phép so mà cả hai dạng trả 0 khắp nơi thì không mang tin). Chênh +0,0498 so với bản gốc **không được đọc là "mạch nhanh hơn"**: nó khớp cả với tán xạ P&R, nên đã phân định bằng cách chạy lại cùng netlist — **+0,2549 trùng khít hai lần**, luồng tất định, ba con số là tính chất CỦA NETLIST. Cơ chế thì không giải thích được, và một con số đo được không tự mang theo cơ chế của nó. ✅ **Cặp kẹp dịch CẢ HAI đầu, nên phải đo lại cả nửa TRƯỢT:** cũ 2,84 = −0,0749 (trượt) → mới **+0,0029 (đóng)**; nửa trượt mới là 2,80 = −0,0331. `Fmax 320,5 → 352,1 MHz`, khoảng thật [352,1 ; 357,1) rộng 1,4 %. Con số cũ **không sai** (chặn dưới vẫn đúng) nhưng **bảo thủ 9,9 %**. Bài báo có bản `-v7.tex`, **không ghi đè** v6. Diện tích: 234 → 251 cell, 3.181,8 → 3.313,2 µm² (**+4,1 %**) — 131 µm² trên một chip 5,02 mm², tức 0,003 %, để đổi một lệnh bị thi hành sai. ✅ Độ phủ `ecg_cvxif` 52,9 → **59,9 %**, và phần còn lại đã phân định: trong 280 điểm chưa chạm **192 là cấu trúc theo hợp đồng** (`assert` dòng 241/244 CẤM lái bit cao; `iss_rs2_i` đã 28/28 = 100 % dải hợp pháp) | `make sim-cvxif`, `make formal`, `90-results/logs/pnr-cvxif-{2p80,2p84,3p12}-enc*.log`, `docs/mot-ma-lenh-la-mot-cap.html` |
| C69 | **`ecg_sram_macro` line 32,0 % la MOT TINH CHAT CUA PHEP DEM, khong mot lo kiem thu** — va bao cao cua chinh du an tung noi nguoc | muc `line` thap nhat kho, va bang khong noi vi sao | ✅ **Ca 34 dong chua chay nam trong `function automatic` duoc luong gia luc ELABORATION** (`kind_rows`, `kind_dw`, `kind_use`, `kind_area`, `kind_plan`, `plan_nbanks`, `plan_kind`, `plan_base` — cac ham hang sinh ra `localparam` cho ke hoach chia bank). Bo dem dong cua Verilator dem luc CHAY, nen chung mai mai bang 0. **BANG CHUNG rang chung DA chay, va no nam trong CUNG bao cao:** `g_bank[0].g_k1.u_ram` va `g_bank[1].g_k2.u_ram` co mat trong phan toggle voi 2.586 bit da lat — hai thuc the do CHI ton tai neu `kind_plan()` da chay va chon loai 1 va 2; neu ham do khong chay thi module khong the duoc elaborate. ⚠️ **Loi that su o day la o BAO CAO, khong o phep kiem:** `tools/coverage_phanra.py` in muc *"DONG CHUA CHAY"* kem cau *"day la thu duy nhat trong bao cao nay ma nguoi doc hanh dong duoc ngay"* — 34 dong khong hanh dong duoc, duoc trinh bay nhu 34 viec phai lam. Da them `dong_trong_ham()`: tach hai loai bang mot phep nhan dien **CU PHAP** (`function`..`endfunction`), va khi khong con dong nao ngoai ham thi bao cao noi thang *"con so `line` thap o day la tinh chat cua PHEP DEM"*. Nhan la "trong function" chu khong "khong chay duoc" — mot ham goi trong `always` thi VAN chay, nen mot nhan hep hon se la mot phep suy khong do duoc. ✅ **Chot chong am-tinh-gia:** kiem lai tren 5 phep khac — `sim-addrgen` (1 dong), `sim-seq` (4), `sim-seq-vec` (3), `sim-mmio` (4), `sim-coproc` (8) — moi dong that VAN hien, khong dong nao bi phan loai lai | `python3 tools/coverage_phanra.py 90-results/coverage/sim-sram-macro.dat` |
| C70 | **Bon duong UART cua `ecg_mmio` chua he duoc testbench DON VI chay** — chung chi duoc kiem o muc HE | do phu line 40/44, bon dong 597/605/650/654 | ✅ Do la ly do lo nay khong lo ra som: `sim-soc` chay loopback UART **256/256 byte** nen duong do "hoat dong", nhung mot bit ket hay mot phep giai ma dia chi lech chi lo ra o muc don vi, re hon nhieu bac. **Dong 605 (`default: ;`) khai mot HANH VI chu khong mot cho trong:** mot dia chi khong dung phai bi bo qua va **KHONG** dat co. T20 them 7 phep kiem; phep phan dinh cho dong 605 la so ERRSTAT **TRUOC va SAU** chu khong doc mot lan — doc mot lan thay 0 thi khong phan biet duoc "khong dat co" voi "co von da 0". Do phu: line **90,9 → 100,0 %**, toggle **64,6 → 68,7 %**. ✅ **Bon dot bien, phan dinh sach:** bo `err_q[11]` (tran gui) → chi T20; doc UART_RX khong RUT → chi T20 (2 loi); `default:` dat co → chi T20; va mot nhom bien mat → **chot san** ban dung chu *"chi 19 nhom chay, san la 20"*. ⚠️ **Chuoi PASS cua tb nay tung KHAI "19 nhom" bang mot hang go tay:** them T20 no van ghi 19, va **xoa** mot nhom no cung van ghi 19. Mot chuoi ket qua duoc LUONG TU HOA boi mot dai luong thi dai luong do phai duoc **DO** — nay dem tu chinh nhan `Tnn` cua tung phep kiem, kem mot san khong duoc tut | `make sim-mmio`, `90-results/logs/sim-mmio-dotbien-T20.log` |
| C71 | **Dong luc toi ghi cho mot phep kiem KHONG phai ly do no co gia tri** — `quet_truong` cua `ecg_desc` | do phu toggle 84,8 %, ba truong chi phu mot phan be rong hop dong (`rq_base` 8/14 bit, `dst_off` 5/10, `len_out` 6/10) | ✅ Bo vector nay lay **54 lop THAT** tu bo bien dich, nen dai gia tri cua moi truong la dai ma bon mo hinh **that su** dung -- khong mo hinh nao dung het 14 bit `rq_base` (do duoc: gia tri that ≤ 255). Da them 40 the QUET TRUONG (moi truong toan-1 tren nen 0, va the bu), phat voi `valid = 0` -- tb VAN kiem tung truong vi `chk("rq_base", ...)` nam TRUOC `if (valid)`. Do phu **84,8 → 99,8 %** (685 → 806/808). ⚠️ **VA DONG LUC TOI GHI BI CHINH DU LIEU BAC BO.** Toi viet rang che do hong can bat la **mot lat bit lech** -- docstring bo sinh ghi ro no TUNG XAY RA (*"khi resv doi tu 14 xuong 13 bit va lam moi truong lech mot bit"*) -- va rang mot lop that co the khong bat duoc neu hai truong ke nhau tinh co mang gia tri giong nhau. **Phep do bac bo dieu do:** dot bien doi cho `w_base` ↔ `rq_base` (cung 14 bit, nen no DUNG duoc, khac ban `dst_off` 10→9 chi cho `%Error` be rong) bi **bo vector CU bat voi 72 loi** -- cac lop that von co `w_base` khac `rq_base`, nen mot phep doi cho la ro rang. ✅ **Lop ma no THAT SU them, tim bang mot dot bien PHAN DINH:** che `rq_base[13]` ve 0 -> bo vector **CU: PASS** (hoan toan vo hinh) · bo **MOI: FAIL 20 loi**. Tuc gia tri that la **cac bit cao ma khong mo hinh nao chay toi**, khong phai lat bit lech. Mot phep kiem co the dung ma **ly do ghi kem cho no thi sai**, va chi mot dot bien phan dinh moi tach duoc hai cai do | `python3 tools/rtl_ref/desc_ref.py && make sim-desc`, `90-results/logs/sim-desc-quet.log` |
| C72 | **Một CHỮ ĐẬM là một lời khai, và bảng KTC của chính bài bác nó** — bài in `m2 ResNet $0.845/\mathbf{0.631}$` (chữ đậm trong bảng kết quả nghĩa là TỐT NHẤT) | P68 nêu "m3 is best inter-patient" là sai; kiểm thì thấy bài **đã** sửa sang m2, nhưng sửa sang một lời khai khác cũng không đỡ được | ✅ **Hai đường đo độc lập, cùng một kết luận.** (1) **Phương sai hạt giống:** m2 hơn m3 **0,45 điểm** trong khi sd gộp (hypot) là **1,14** — nhiễu lớn **gấp 2,5 lần** khoảng cách. (2) **Bootstrap mức BỆNH NHÂN** (`val-ci.csv`, DS1-val 5 bệnh nhân, 1000 lần lấy lại): cặp m2/m3 có KTC 95 % **[−7,08 ; +2,03]** — vắt qua 0, rộng 9,11 điểm — và **cả sáu** cặp đều ghi `distinguishable = khong`. ⚠️ **Và bài TỰ MÂU THUẪN:** dòng 450 viết *"No architecture gap survives a patient-level test"* trong khi bảng in đậm một họ. **Người phản biện thấy chữ đậm trước.** Bỏ chữ đậm làm bảng KHỚP văn bản — phép sửa là **xoá một lời khai**, không thêm một câu. ✅ **Phán định phải TÁCH hai cột, không gộp:** chữ đậm `\mathbf{0.860}` (within-record của m3) thì **GIỮ** — khoảng cách **1,10 điểm** so với sd gộp **0,94**, tỉ số **1,17**, tức nó VƯỢT nhiễu. Một phép sửa gộp cả hai sẽ xoá một lời khai đúng. ⚠️ **Và bài KHÔNG nhận thêm một câu giải thích:** bản thử thêm mệnh đề nêu cơ sở (+51 ký tự) làm bài thành **5 trang / 1 overfull**. Bài nhận PHÉP SỬA (−9 ký tự, 4 trang, 0 overfull); LẬP LUẬN ở trong kho. Một bài 4 trang sát giới hạn thì **chỗ trống cũng là một ràng buộc thiết kế**, và lời giải là chuyển lập luận sang nơi có chỗ, không nén nó đến mức mất nghĩa | `python3 tools/checks/paper-ban.py`, `90-results/tables/val-ci.csv`, `benchmark.csv` |
| C73 | **Cổng `paper-ban` chưa từng chạy trong `make checks`** — và chú thích của chính nó nói ngược | chú thích ghi *"Phải ở đây chứ không một lệnh riêng: một cổng không ai gọi thì ngang một cảnh báo trong chú thích (bất biến 37)"* | ✅ Đo được: `paper-ban.py` được gọi từ đích **`paper-check`**, và `paper-check` **chỉ xuất hiện trong `.PHONY`** — không đích nào phụ thuộc nó, kể cả `checks`. Nên cổng chạy khi và chỉ khi một NGƯỜI gõ `make paper-check`, tức **đúng điều kiện mà chú thích của nó nói là nó tránh được**. Chú thích sai về chính tác dụng của mình, và nó sai theo hướng làm người đọc yên tâm. Đã thêm `tools/checks/paper-ban.sh` (luật `checks:` quét `tools/checks/*.sh`). ✅ Chứng minh đỏ được bằng đúng điều luật 2 cấm: nối một dòng vào `iscas2027_ecg-v5.tex` → rc=1, *"v5 ĐÃ BỊ SỬA: sổ bản ghi 84ead8a1… mà tệp này 60d57e90… Bản cũ là BẤT BIẾN"*. ⚠️ **Và phép hoàn nguyên của tôi làm nó đỏ tiếp vì một lý do KHÁC:** `cp` khôi phục nội dung nhưng đặt mtime mới, làm `.tex` mới hơn `.pdf` — luật 1 bắt đúng điều đó. Hai lần đỏ liên tiếp **không cùng một nguyên nhân**, và đọc lướt sẽ tưởng phép hoàn nguyên thất bại | `bash tools/checks/paper-ban.sh` |
| C74 | **Một `base = 0` cố định làm một đường địa chỉ BỊ RỚT trở nên vô hình** — `tb_ecg_seq.cpp:179-180` chốt cứng `w_base_i = 0` và `rq_base_i = 0` | độ phủ chỉ ra `w_base_i` / `w_base_q` / `rq_base_i` / `rq_base_q` **chưa chạm 100 %** — 110 điểm toggle, **24 %** của phần chưa phủ | ✅ **Và lớp này ĐÃ cắn chính module này rồi:** chú thích `ecg_seq.sv:116-118` ghi *"ban đầu ở đây dùng `dst_off` TRỰC TIẾP, và bộ vector luôn đặt `dst_off = 0` nên phép kiểm không phân biệt được"*. Cùng một hình dạng, một trường khác — nên giả thuyết không phải suy đoán. ✅ **Cách kiểm không đổi kỳ vọng:** tb tra mảng của nó bằng chỉ số **đã trừ base**. RTL cộng base đúng thì hai phép triệt tiêu và kết quả không đổi; RTL rớt base thì chỉ số lệch. Nên phép thử chỉ làm đường địa chỉ **phải thật sự mang base**. ✅ **ĐỘT BIẾN PHÂN ĐỊNH, và nó là ca sạch nhất trong nhóm này:** che `w_base_q` về 0 trong `w_off_o` (dạng `& {WBITS{1'b0}}` — **giữ nguyên bề rộng nên nó DỰNG ĐƯỢC**, 0 lỗi dựng) → bộ tb **CŨ: PASS** (hoàn toàn vô hình) · bộ **MỚI: FAIL, 67/73 lớp sai**. Độ phủ **87,8 → 90,3 %**. ⚠️ **Hai đột biến trước đó phải BỎ vì chúng không dựng được:** xoá hẳn `w_base_q +` cho `%Error` bề rộng, và một `rc=2` từ lỗi dựng khớp với cả "phép thử bắt được" và "không biên dịch" — tức không mang tin. Dạng CHE giữ mọi bề rộng là dạng duy nhất phân định được. ⚠️ **Và phép so đầu của tôi VÔ HIỆU:** bản sao "tb cũ" tôi lưu lại là bản ĐÃ VÁ (lưu sau khi sửa), nên hai lần chạy là cùng một bộ. Bản cũ thật phải lấy từ `git show HEAD:` | `make sim-seq`, `90-results/logs/` |
| C75 | **`ecg_wmem` 93,8 % tách thành CẤU TRÚC và LỖ THẬT, và phép tách là ĐO TỪNG BIT** — không phải đọc một tỉ lệ | 84 điểm chưa chạm, và một con số gộp không nói được nên làm gì | ✅ **Cấu trúc — không đường nào chạm tới:** `we_idx`/`wo_idx` lật bit 0–8 và chỉ số bank tối đa là **291** (292 từ/bank) tức **đúng 9 bit**; `wr_word_q` lật 0–9 với tối đa **582 từ** tức 10 bit; `dma_len_i` bit 0,1,2 luôn 0 vì cổng ghi *"byte, bội của 8"*; và bit 13 = **8.192 > ECG_WMEM_BYTES = 4.664**. Mỗi kết luận là một phép tính, không một cảm giác. ✅ **Lỗ thật:** `dma_len_i` chỉ lật bit **3,4,5,9,12** — **đúng các bit của MỘT giá trị duy nhất**, `4664 = 0b1001000111000`, vì bộ vector có một dòng đầu `<n_bytes>` nên cả lần chạy chỉ thử **một** độ dài. `wr_words_q` lật 0,1,2,6,9 = các bit của `4664>>3 = 583`. ✅ **Năm độ dài thêm vào được CHỌN theo các bit còn thiếu, không cho đẹp:** 64/128/256/1024/2048 lật bit 6/7/8/10/11 của `dma_len_i`, và chia 8 cho 8/16/32/128/256 tức bit 3/4/5/7/8 của `wr_words_q` — đúng tập con thiếu của **cả hai** tín hiệu. Độ phủ **93,8 → 95,3 %**. ✅ **ĐỘT BIẾN PHÂN ĐỊNH:** kẹt bit 7 của `dma_len_i` (dạng ghép `{dma_len_i[OFF_W:8], 1'b0, dma_len_i[6:0]}` — giữ bề rộng nên **dựng được**) → bộ tb **CŨ: PASS** (bit 7 của 4664 vốn đã là 0 nên nó **không thể** thấy) · bộ **MỚI: FAIL**. ⚠️ **Và bản đột biến đầu phải bỏ** vì mặt nạ `~{{(OFF_W-6){1'b0}}, 1'b1, 7'b0}` cho `%Error` — lần thứ hai trong tick, và lần thứ tư trong phiên: **một đột biến phải DỰNG ĐƯỢC trước khi nó là một đột biến** | `make sim-wmem`, `90-results/logs/` |
| C76 | **Mô hình chu kỳ tuyến tính theo số MAC; RTL có một SÀN 11 chu kỳ mỗi (nhóm kênh ra, vị trí đầu ra)** | trục 1: m4 đo 36.562 mà mô hình cho 30.255, và chưa ai truy khoảng cách đó | ✅ **Bước 1 — vòng MAC KHÔNG phải nguồn:** chạy từng lớp MAC của m4 trên `tb_ecg_seq` (một tệp vector một lớp), **tám trên mười** tốn đúng `mô hình + 15` chu kỳ. Chỉ `stem.0` lệch **+1.035**. ⚠️ **Bước 2 — giả thuyết đầu BỊ BÁC:** *"cin = 1 làm vòng trong không che được đường ống"* — `(cin=1,k=7)` và `(cin=7,k=1)` cho **đúng cùng 2.827**. Cái chung là **tích** `cin×k`, không `cin`. ✅ **Bước 3 — ngưỡng ở ĐÚNG 11:** quét `cin×k` 4→16 cho `2824 2825 … 2831 3087 3343 4111` — phẳng tới 11 rồi tuyến tính; hai nhánh giao tại `255x = 2805` → **x = 11,0**. Và đây **không** phải quan hệ theo liều: chu kỳ **không giảm** khi MAC giảm, nó chạm sàn và dừng — một đường phẳng rồi gấp khúc là chữ ký của **ngưỡng**, và hai thứ đó đòi hai kết luận khác nhau. ✅ **Bước 4 — sàn tỉ lệ với CÁI GÌ**, vì 2.828 có thể là phí cố định mỗi lớp: đổi `len_out`/`cout` thì `đo/(nhóm×vị trí)` giữ **11,02–11,12**, và hai hình dạng khác nhau có **cùng tích** cho **đúng cùng 1.416** chu kỳ. Nên sàn tỉ lệ `⌈cout/8⌉ × len_out`, không cố định mỗi lớp. ✅ **Giá: 1.040 chu kỳ mỗi họ, y hệt cả bốn** (cùng stem + cùng đầu mã hoá RR) — 2,8 % của m4, 6,5 % của m1, và nó giải thích ~16 % khoảng cách mô hình/RTL; phần còn lại ở **mức coproc**, không ở vòng MAC — tức *tối ưu vòng MAC không phải chỗ để tìm*. ⚠️ **HỆ QUẢ CHO SỐ PE, quan trọng hơn 1.040 chu kỳ:** khi tăng `pe` thì `⌈cout/pe⌉` giảm nhưng **sàn mỗi vị trí không giảm**, nên `cycle_model.py` — công cụ *được dùng để chọn số PE* — **ước lượng quá cao lợi ích của thêm PE**, và lạc quan nhiều nhất ở đúng những lớp sàn đang chi phối. 📌 **KHÔNG sửa mô hình:** hai đầu mút `9,4 %`/`23,0 %` của bài được `build_phandinh.py` dẫn **từ chính các hàng `T_infer`**, nên hấp thụ sàn là đổi hai con số của bài và một bản mới — một quyết định về BÀI BÁO, không về mã | `python3 tools/do_san_chu_ky.py --check`, `docs/mot-san-11-chu-ky.html` |
| C63 | **DRC magic: 269 vi phạm THUỘC THIẾT KẾ, phần còn lại là hiện vật của đường DEF→magic** | "5.537 vi phạm DRC" | ✅ Phân rã theo luật: **met3.6** (diện tích tối thiểu met3) **269**, **met1.2 = 0**. Số cũ **264 + 143 = 407** là của `-droute_end_iter 5`; nâng lên **64** (nay là mặc định, biến `ECG_DRT_ITER`) đưa TritonRoute về **0** và **cả 143 met1.2 biến mất cùng lúc** — chúng chưa bao giờ là hạng mục riêng mà là phép phân rã mịn hơn của cùng chỗ: cả 143 dấu nằm trong 1 µm của một vi phạm TritonRoute. Giả thuyết mạnh nhất — PDN followpin xung đột met1 tín hiệu — **bị bác bỏ bằng toạ độ**: không dấu nào nằm gần dải nguồn met1 (cách tâm followpin 1,0–1,6 µm, tức giữa hàng cell), và 39 vi phạm TritonRoute đều là **tín hiệu với tín hiệu**. Phép đổi sang `met2-met5` vẫn đã chạy và **không sửa được gì** mà sinh **60 vi phạm met5.1 mới**, nên mặc định giữ `met1-met5`. **269 met3.6 còn lại không sửa được bằng số vòng**: chúng đến từ định nghĩa via của chính nền tảng — `M2M3_PR` miếng met3 0,33×0,33 = 0,1089 µm² < 0,24 và `M3M4_PR` 0,38×0,32 = 0,1216 µm² < 0,24 — nên **mọi** via đứng một mình là một vi phạm theo đúng cấu tạo (215/264 dấu nằm trong 0,2 µm của một via M2M3/M3M4). Bản OpenROAD này không chèn miếng vá min-area, nên TritonRoute báo 0 còn magic báo 269: hai công cụ đo hai thứ khác nhau, cả hai đều đúng. **Giá phải trả: 0,0009 ns slack** (+1,0517 → **+1,0508 ns**, cả hai CÓ lưới nguồn), vì sửa DRC bằng cách đi vòng và đường vòng thì chậm hơn; diện tích 88.419 µm² không đổi. **SUA LAI (phan dinh bang hai lieu sach, moi lieu doi DUNG MOT bien):** cap `+1,3409 -> +1,0508` TRON hai bien -- 1,3409 la it5 KHONG luoi nguon con 1,0508 la it64 CO luoi nguon. Ba luot chay cung mot lenh `tools/asic-flow.sh pnr ecg_seq 24.53 45` cho: 1,3409 (pdn=0, 6 vong, 39 vi pham) · 1,0517 (pdn=1, 6 vong, 50) · 1,0508 (pdn=1, 15 vong, 0). Nen **1,3409-1,0517 = 0,2892 la gia cua LUOI NGUON** va **1,0517-1,0508 = 0,0009 la gia cua SO VONG dinh tuyen**, tuc gia cua viec sua DRC chi la 0,0009 ns chu khong 0,29. Dien tich 88.419 khong doi o ca ba. Bai bao (dong 414-415) da ghi DUNG; chinh o nay va ORCHESTRATOR-STATE P25 sai cung mot kieu. Ba dong do nay nay o `90-results/tables/asic-seq-pnr.csv` voi hai cot moi `drt_vong_chay`/`drt_vi_pham_con`. Đối chứng đã chạy trước khi tin số mới: chạy lại DRC trên đúng DEF cũ (`-droute_end_iter 5`) bằng đường mã hôm nay ra lại **chính xác** 5.569 / 264 / 143, nên chênh lệch là do đổi luồng chứ không do đổi cách đo. ⚠️ **Tổng của cấu hình CHỐT là 5.537, không phải 5.569** — xem C64: hai con số đó thuộc hai `-droute_end_iter` khác nhau, và bài báo từng ghép 5.569 (it5) với 269 (it64). Phần còn lại là luật giếng/cấy/latch-up, và có **hai bằng chứng** chúng là hiện vật: (a) một cell chuẩn **của nhà cung cấp** đứng cô lập (`sky130_fd_sc_hd__inv_1`, đọc từ GDS của chính nền tảng) báo **3** vi phạm, và đúng là `nwell.4` + `LU.3` + `LU.2` — luật latch-up mà một cell cô lập **không thể** thoả theo cấu tạo; (b) **`hvi.5` báo 5.612 vi phạm** (khoảng cách lớp cấy **điện áp cao**) trong một thiết kế dùng **chỉ** `sky130_fd_sc_hd__` (1,8 V) và **không một** cell cao áp — đã kiểm bằng cách liệt kê mọi tiền tố cell trong DEF. Một luật về lớp không tồn tại trong thiết kế không thể bị thiết kế vi phạm | `tools/asic-drc-lvs.sh drc` |
| C64 | **Hai tổng DRC đo hai thứ khác nhau — VÀ mỗi cặp thuộc một CẤU HÌNH** | cộng chúng lại; hoặc ghép tổng của cấu hình này với phân rã của cấu hình kia | ⚠️ Hai chiều phải nêu cùng lúc. **Chiều thứ nhất — cách đếm:** `drc list count total` đếm *chỗ*, còn cộng số box theo từng luật đếm *lần bị tính*, và một box có thể bị tính dưới nhiều luật; hai số luôn lệch theo cấu tạo. **Chiều thứ hai — cấu hình:** cả hai đổi theo `-droute_end_iter`, đo được: `it5` (đối chứng) **5.569 / 11.063 / met3.6 264 / met1.2 143** · `it64` (**CHỐT**) **5.537 / 10.925 / met3.6 269 / met1.2 0** · `it64 + met2–met5` (đã loại) **5.565 / 10.933 / met3.6 217 / met1.2 0**. Lưu ý met3.6 **tăng** 264 → 269 khi nâng số vòng: thêm vòng định tuyến cho hình học khác đi chút, nên phân rã theo luật **không** đơn điệu theo số vòng dù tổng thì giảm. **Lỗi thật đã xảy ra:** bài báo in *"Magic lists 5.569, of which 269 are the design"* — 5.569 là của `it5` còn 269 và "0,29 ns slack" là của `it64`, tức một câu ghép hai cấu hình. Đã sửa thành **5.537 / 10.925** (`0cff7e1` → bản sau). Trích một tổng mà không nói **cả** cách đếm **và** `-droute_end_iter` thì con số vô nghĩa | `drclvs-drc-control-base-20260902T031939Z.log` (it5), `drclvs-drc-it64-20260902T032321Z.log` (**chốt**), `drclvs-drc-it64m2-20260902T025758Z.log` |
| C65 | **Lần thứ sáu cùng lớp lỗi: GDS cũ được dùng lại** | — | ❌→✅ Giai đoạn DRC chỉ sinh lại GDS khi tệp **không tồn tại**, không khi DEF mới hơn. Sau khi chạy lại P&R có PDN, DRC đọc GDS cũ (07:28) của DEF mới (08:28) và báo **đúng 5.479** như trước — **dấu hiệu là chính sự giống hệt đó**: một con số không dịch khi layout đã đổi thật sự thì đáng nghi ngang một con số vô lý. Sửa bằng phép so `-nt`; sau khi sinh lại là 5.569 | `tools/asic-drc-lvs.sh` |
| C58 | **`detailed_route` CHẠY ĐƯỢC — nguyên nhân là thiếu tệp guide, và cờ đúng tên là `-guide`** | năm giả thuyết đã bị loại bằng phép đo | ✅ Không truyền guide thì TritonRoute chạy **bộ định tuyến toàn cục RIÊNG của nó** (`fr::FlexGR`), và chính FlexGR báo `initial #node != #rpin`. Cờ đúng là **`-guide`**; `-guide_file` **chưa bao giờ tồn tại** trên `detailed_route` của bản này. Giả định cũ trong `asic-pnr.tcl` — "`global_route` lưu guide trong DB và `detailed_route` dùng trực tiếp" — **SAI**: guide không đi từ `grt` sang `drt` qua DB. Sửa: `write_guides` ngay sau `global_route` (phải **cùng phiên** — sau `read_db` thì `write_guides` segfault vì `grt` không đọc DB), rồi `detailed_route -guide`. Bằng chứng: cùng một ODB, không `-guide` → gãy sau 19 s; có `-guide` → 59.347 guide, chạy hết | log `drt-repro-*`, `pnr-drt-spef-*` |
| C59 | **Kết quả sau định tuyến chi tiết, ký sinh THẬT** | mọi số trước đây là sau global route | ✅ `ecg_seq` @ 24,53 ns: **slack +1,3409 ns**, TNS 0, **39 vi phạm DRC** (23 MetSpc + 16 Short, toàn met1), diện tích **88.419 µm²** ở 53 % util, dây **238.892 µm**, **66.291 via**. Hội tụ DRC qua 5 vòng: 10031 → 4219 → 1088 → 165 → **39** (`-droute_end_iter` mặc định của ORFS là 64, nhiều khả năng về 0). Có PDN (`pdn=1 drt=1 spef=1`, util 45, log `pnr-pdn-full-20260902T003734Z`): **50 DRC, slack +1,0517 ns** — nhưng đó là `-droute_end_iter 5`; với mặc định mới **64** thì TritonRoute về **0** và slack **+1,0508 ns** (C63). Số cũ ghi ở đây — 77 DRC / +0,3247 ns — là của một tcl rời chạy trước trong scratchpad, KHÔNG phải lần chạy dứt điểm; giữ lại trong chú thích này vì nó đã lọt vào bản nháp bài báo một lần | `70-asic/build/pnr-ecg_seq-24.53/` |
| C60 | **`estimate_parasitics` KHÔNG có `-detailed_routing` trong bản này — gọi sau route trả về số TRƯỚC route** | tin rằng nó cập nhật | ⚠️ Chỉ có `-placement` và `-global_routing`. Gọi sau `detailed_route` trả về **đúng con số trước khi định tuyến chi tiết** (+0,2618 ns cả hai lần). Số post-route thật phải qua **OpenRCX** (`define_process_corner` → `extract_parasitics -ext_model_file` → `write_spef` → `read_spef`; lưu ý `set_extraction_rules_file` **không tồn tại**). Cùng lớp với C39/C43/C56: một bước trả về thành công nhưng cho số cũ | `tools/asic-pnr.tcl` |
| C61 | **Tôi đã nói SAI CHIỀU: ký sinh thật cho slack TỐT HƠN ước lượng global route** | mọi chỗ trích số đều ghi "số cuối cùng sẽ xấu hơn" | ❌→✅ Đo được: **+1,3409 ns** (SPEF thật) so với **+0,2618 ns** (ước lượng GR) ở cùng chu kỳ — GRT **bi quan** ở thiết kế này. Nên **40,7 MHz không phải một số lạc quan cần hạ xuống mà là một số bảo thủ đã được xác nhận sau định tuyến chi tiết**, còn dư 1,34 ns. Ngoại suy cho ~23,19 ns ≈ **43,1 MHz**, nhưng đó là ngoại suy chứ không phải phép quét (mỗi điểm quét sau route mất ~47 phút) | `pnr-drt-spef-*` |
| C62 | **480 vi phạm max_slew, 446 trong đó ở `RESET_B`** | — | ⚠️ `rst_ni` có fanout lớn và **không có cây đệm**. Đây là vấn đề riêng và sửa nó sẽ đổi diện tích lẫn tần số toàn dự án, nên chưa sửa. **Hạng mục mới** | `pnr-drt-spef-*` |
| C55 | **DRC/LVS: chuỗi công cụ chạy được, nhưng KHÔNG có layout nào để kiểm** | tưởng chỉ cần chạy | ⚠️ Đã quét **cả 70** tệp `.def` trong `70-asic/build/pnr-*/`: **tất cả `ROUTED` = 0**, `SPECIALNETS` = 0. Nhất quán với nhãn "sau **global** route" (`estimate_parasitics -global_routing` dùng *ước lượng*, không dùng dây thật) nhưng nghĩa là chưa có gì để DRC. Thiếu theo thứ tự: (1) PDN, (2) `detailed_route`, (3) GDS mức đỉnh. Chuỗi công cụ đã kiểm chạy được trên `ecg_cvxif`: magic nhận đúng 26 loại cell, netgen báo `Device classes … equivalent`. Giai đoạn `kiem-dinh-tuyen` **dừng hẳn mã 3** thay vì để DRC chạy — DRC trên layout chưa route sẽ **đạt** vì cell chuẩn vốn DRC-clean, và "0 vi phạm" ở đó là con số **sai** | `tools/asic-drc-lvs.sh` |
| C56 | **`gds readonly true` hỏng IM LẶNG và tạo phép "đạt" hoàn toàn giả** | cách OpenLane hay dùng | ❌→✅ magic coi cell đỉnh là vendor cell → `Calma output error: Structure name not found` → GDS lỗi cấu trúc → đọc lại cho `Expected ENDSTR record but got BGNSTR` → `There is nothing here to extract`. Hậu quả: **DRC báo 0 vi phạm và LVS không có gì mà so**. Sửa thành `gds readonly false`. Cùng lớp với C39 (blob sai im lặng) và C43 (báo cáo rỗng im lặng) | `tools/asic-drc-lvs.sh` |
| C46 | **Tần số CẦN bao nhiêu — và nó là 13,16 MHz, không phải 50** | ngưỡng phát biểu bằng chu kỳ nên chưa ai tính ra Hz | ✅ Hạn chức năng (ADR-0002 §4.3) là **một chu kỳ lấy mẫu = 2,78 ms ở 360 Hz**; T_infer xấu nhất **đo được** 36.562 chu kỳ (m4) → **cần 13,16 MHz**. **Toàn chip `ecg_coproc` với ba macro SRAM thật, sau ĐỊNH TUYẾN CHI TIẾT và ký sinh SPEF thật: đóng ở 34,0 ns (slack +0,1639), TRƯỢT ở 33,5 ns (−0,0861) → 29,4 MHz, biên 2,23×.** Đây là số **kẹp** (có chu kỳ đóng và chu kỳ trượt liền kề), không phải nội suy. ⚠️ **Và một cặp kẹp chỉ mạnh bằng ĐỘ RỘNG của nó (bất biến 29):** khoảng thật là **[29,4 ; 29,9) MHz**, rộng **1,5 %**. Bảng ghi số đã **cắt** chứ không làm tròn (`1000/34,00 = 29,41 → 29,4`), tức đúng chiều bảo thủ cho một chặn dưới. Độ rộng của mọi cặp kẹp: lõi **0,6 %** · `ecg_seq` 1,2 % · chip 1,5 % · `ecg_mac8` 3,4 % · `ecg_addrgen` 4,3 % · `ecg_vecop` 4,7 % · `ecg_cvxif` **9,9 %**. Nên biên `≥2,23×` và `≥3,09×` an toàn (chặn dưới ở **tử**), nhưng **tỉ lệ của hai cặp kẹp không phải chặn theo chiều nào**: "MAC nhanh hơn requant 2,7×" thật ra là **[2,64 ; 2,79)** và đã sửa thành `2,6–2,8×`; "requant chia tầng 1,78×" là **[1,74 ; 1,82)** → `1,74–1,82×`. ✅ **Phép so KHỐI với CHIP nay là hai phép ĐO ở CÙNG MỘT CHU KỲ, không còn suy luận:** tại **33,5 ns** `ecg_coproc` có slack **−0,0861** (trượt) còn `ecg_seq` có **+10,0208** (đóng, `tns 0`) — đo bằng STA trên DEF `pnr-ecg_seq-24.53-it64` đã có, **16 giây**, vì hình học không phụ thuộc chu kỳ nên không cần P&R lại (`sta-quet-ecgseq-33p5-20260902T151948Z.log`). `ecg_seq` là đường **TRỌN** chu kỳ (khác coproc): `slack = P − 23,4792` khớp **năm điểm, độ lệch 0,0000**. ⚠️ Điểm 36 ns lệch +0,0354 vì **đổi nhóm đường** sang đường qua cổng `0,9P − 19,9146` (do `set_input/output_delay = P × io_frac`), giao tại **35,646 ns** — ai quét trên mốc đó sẽ thấy độ dốc khác và dễ tưởng là lỗi đo. Cùng hiện tượng P35 tìm ở lõi (giao 22,32 ns), tức tính chất của **SDC** chứ không của một thiết kế. ✅ **Và cặp kẹp đã được đo TRÊN HAI LAYOUT ĐỘC LẬP, cùng ra một kết quả:** layout A (`pnr-ecg_coproc-sram-46u50`) cho 34,0 **+0,1639** / 33,5 **−0,0861**; layout B (`pnr-ecg_coproc-sram-36`, netlist nguồn khác — `route.guide` md5 `599121d1…` so với `34a03206…`) cho 34,0 **+0,1984** / 33,5 **−0,0516**. Hai layout, hai netlist, hai lần định tuyến chi tiết, **cùng 29,4 MHz**. Độ dốc 0,5 xác nhận lại trên B (36 → 34,5 → 34 cho 1,1984 → 0,4484 → 0,1984). Điều này **đóng phần lớn khoảng hở "bậc 3"** ghi ở dưới: con số công bố nay không còn dựa vào suy luận "hình học độc lập chu kỳ" nữa, vì nó đã được đo trực tiếp ở hai layout. Ở global route cùng thiết kế: đóng 36 ns / trượt 35,5 ns → 27,8 MHz — ký sinh thật làm slack **TỐT hơn** 0,9255 ns (c: 17,7616 → 16,8361), nên bản ước ở global route là **bi quan**, không phải lạc quan như tôi từng giả định. ⚠️ **`T − WNS` KHÔNG cho Fmax ở đây, sai vì HAI lý do độc lập:** (a) công cụ chỉ tối ưu tới ràng buộc nên slack dư không đo được năng lực; (b) **đường tới hạn là đường NỬA CHU KỲ** — `dout1` của macro SRAM có `timing_type: falling_edge` với `related_pin: clk1`, nên `slack = P/2 − c`, không phải `P − c`. Đo bằng đường thứ hai: quét 7 điểm STA (46/40/36/34/33,5/33/32 ns) cho độ dốc **đúng 0,5 ns slack trên 1 ns chu kỳ** ở cả 7 điểm, mô hình `P/2 − 16,8361` khớp với độ lệch **0,000000**, còn mô hình trọn chu kỳ lệch trải 7 ns. Đọc trọn chu kỳ sẽ đặt điểm cắt **lệch 6 ns**. ⚠️ **"Dư 3,09×" là số của `ecg_seq` MỘT MÌNH (40,7 MHz), KHÔNG phải của chip** — tỉ lệ khối/chip là 29,4/40,7 = **0,72×**. ⛔ **Mọi tần số FPGA của phiên Vivado là giới hạn reg→reg, KHÔNG phải Fmax của hệ có chân thật:** `impl_sweep.tcl` chỉ có `create_clock`, không `set_input_delay`/`set_output_delay`, nên đường qua cổng **không bị ràng buộc** và bị loại khỏi WNS — `check_timing` đếm `ecg_seq` **199/84** cổng thiếu, `ecg_coproc` **282/63**, lõi **384/351**. Bên tôi thì `asic-sdc.tcl` đặt `io_frac = 0,1` **mỗi phía, tỉ lệ theo chu kỳ**. Hai bên hạch toán I/O khác nhau, và không ai báo lỗi — cùng lớp với instance-vs-Slice LUT. ✅ **Nhưng ở hai điểm làm việc chịu lực thì chênh đó ĐO ĐƯỢC là không đáng kể:** `ecg_coproc` 29,4 MHz có đường tới hạn **nội bộ** (macro `dout1` → logic), mô hình `P/2 − 16,8361` khớp 7 điểm **không có số hạng io_frac**; `ecg_seq` 40,7 MHz có độ dốc **đúng 1,0000** trên 5 điểm, còn đường qua cổng cho **0,9** — nên độ dốc 1,0 **chứng minh** reg→reg. Ngoài vùng đó (trên 35,646 ns) thì hai bên rẽ nhau. 📌 **Bài báo trích KHÔNG một tần số FPGA nào** (đã kiểm 52,9 · 62,9 · 63,9 · 66,1 · 66,8 · 70,9 · 67,6 — không cái nào có mặt); phần FPGA còn lại là **tài nguyên**. **Hai đường độc lập cùng một dấu (bản cũ, giữ làm lịch sử):** phiên Vivado thấy `ecg_seq` **70,9** MHz nhưng `ecg_coproc` **66,8** MHz — tỉ lệ **0,94×**. ⚠️ **Cả hai con số này là SAU TỔNG HỢP, `T − WNS` ở T = 10 ns** — tức đúng công thức mà chính ô này nói không cho Fmax. ⛔ **VÀ CẶP NÀY KHÔNG DÙNG ĐƯỢC — tôi từng biện minh rằng "thiên lệch hệ thống triệt tiêu khi chia hai số cùng giai đoạn", và điều đó bị chính dữ liệu của dự án bác bỏ.** Hai trường hợp duy nhất có cả hai giai đoạn cho **dấu ngược nhau**: `cv32e40x_core` 49,7 → 52,9 (**+6,4 %**, bi quan) và `ecg_coproc` 66,8 → 62,9 (**−5,8 %**, lạc quan). Thiên lệch **không hệ thống** nên nó **cộng dồn**: với ±6 % dấu độc lập, `66,8/70,9 = 0,942` nằm trong **[0,834 ; 1,064]**, khoảng **chứa 1,000** → không loại được khả năng khối lớn *không* chậm hơn module lá, tức chính điều cặp số dùng để chứng minh. **Bất biến 28: cùng giai đoạn là điều kiện CẦN, không phải ĐỦ** — nó chỉ loại sai số *lệch giai đoạn*, không loại sai số riêng của từng số. Đã **rút** mệnh đề khỏi bài báo và thay bằng một **bất đẳng thức** (sau DRT giới hạn của `ecg_seq` cao hơn 40,7 nên khoảng cách khối→chip ít nhất bằng 40,7 → 29,4). Số **kẹp** sau P&R của `ecg_coproc` là **62,9 MHz** (15,909 đóng / 15,510 trượt), và **không có** lần impl nào cho `ecg_seq` trên `xc7z020` nên **không có cặp kẹp cùng giai đoạn** để thay. Bài báo từng in `70,9 → 62,9` — trộn cả giai đoạn cả module — nay là `70,9 → 66,8, both post-synthesis`. Hai tỉ lệ lệch nhiều (0,72 so với 0,94) và điều đó **hợp lý**: phía ASIC có ba macro SRAM thật với SPEF thật và 1.461.340 µm dây, phía FPGA thì BRAM nằm sẵn trên fabric nên đường tới hạn không đi qua ranh giới macro. Cùng *dấu*, không cùng *độ lớn*. 📌 **Ở luồng này "STA trên DEF cố định" và "quét P&R lại" là CÙNG MỘT đại lượng — nhưng bằng chứng có BA BẬC và bậc ba là suy luận:** **(1) đo:** năm lần P&R ở 32/34/35,5/36/38 ns cho DEF, netlist và guide **trùng md5 từng byte** (`def=35113082d637828aa6cbae67ae2d32c9`) — nhưng đó là đầu ra **sau GLOBAL route**; **(2) đo bằng đọc mã:** `tools/asic-pnr.tcl` gọi `repair_design` mà **không** gọi `repair_timing`, và lệnh `detailed_route -guide … -output_drc … -droute_end_iter …` **không nhận tham số thời gian nào**; **(3) suy:** vậy hình học sau detailed route cũng độc lập chu kỳ. Định tuyến chi tiết chỉ chạy **một lần** (46 ns), nên cặp kẹp 34/33,5 là STA trên DEF cố định; đóng bậc 3 bằng phép đo cần một lượt DRT thứ hai ở 34 ns (≈1 giờ, **chưa làm**). Hệ quả đã dùng được: bài học Vivado *"công cụ nhân đôi logic khi bị siết"* (4.294→4.316 Slice LUT) **KHÔNG chuyển sang đây**, vì cơ chế gây ra nó (`repair_timing`) không có ở đây. Kết luận: **tần số không phải ràng buộc; biên là 2,23× ở mức chip**, và mọi chỗ trích 3,09× phải nói rõ đó là `ecg_seq` một mình | `asic-freq.csv` (hai dòng `ecg_coproc+sram-gr` / `-drt-spef`), `asic-pnr.csv`, `90-results/logs/p27-sta-quet-drt-coproc-20260902T110553Z.log`, `90-results/logs/p27-md5-hinh-hoc-theo-chu-ky-20260902T113909Z.log` |
| C47 | **~~Chia tầng ống bộ nhân requant~~ — ADR-0018 BÁC BỎ** | tưởng là đường duy nhất tăng tần số | ⚠️ Bản thử nghiệm `40-rtl/est/ecg_requant_p2.sv` **chạy được và đo được**: 20,93 → **13,27 ns** (×1,59), diện tích +1.658 µm² = **+0,07 % chip**, +53 flop (khớp đúng: 36 rounded + 5 shift + 9 bias + 2 act + 1 valid). Hệ thống sẽ về ~16,71 ns ≈ 60 MHz (×1,47). **Nhưng bác bỏ vì tần số đã dư 3,09× (C46)** — ×1,47 mua được đúng 0 về chức năng, trong khi phải sửa 3 module RTL, viết lại 3 thuộc tính formal (cả ba giả định độ trễ 1 chu kỳ), 3 tệp tham chiếu, `cycle_model.py`, và sinh lại toàn bộ vector. **Giữ bản thử nghiệm** làm phương án đã đo sẵn: mở lại nếu lấy mẫu ≥ 2 kHz (cần 73,1 MHz), T_infer > 113.000 chu kỳ, hoặc ≥ 4 kênh đồng thời (52,6 MHz) | ADR-0018 |
| C48 | **Hạ tần số về đúng đủ để giảm công suất — hướng NGƯỢC LẠI mới đúng** | công suất chỉ đo ở tần số tối đa | ✅ `ecg_seq`: **4,77 mW @ 40,7 MHz** → **1,54 mW @ 13,16 MHz**. Tỉ lệ 3,10 khớp tỉ lệ tần số 3,09 → công suất động tuyến tính; rò rỉ **30,4 nW** ở cả hai điểm, không đáng kể ở 130 nm | `asic-power.csv` |
| C49 | **Năng lượng mỗi nhịp = 4,28 µJ, và nó BỀN theo tần số** | chưa có chỉ số so được với đối thủ | ✅ 4,29 µJ @ 40,7 MHz và 4,28 µJ @ 13,16 MHz — không đổi, vì công suất động ∝ f trong khi thời gian ∝ 1/f và rò rỉ không đáng kể. Đây là con số đúng để so với đối thủ (Zhang 2023: 2,05 và 14,27 µJ/suy luận; Mao: 0,3 µJ/nhịp cho SNN) thay vì mW. **Giới hạn**: chỉ `ecg_seq`, **không gồm SRAM** | `asic-power.csv` |
| C44 | **Công suất với hoạt độ ĐO ĐƯỢC, không phải mặc định** | E2-3 chờ từ lâu; mọi tối ưu chỉ được biện minh theo diện tích | ✅ `ecg_seq` ở **40,7 MHz**, hoạt độ **0,2112** lần đổi/bit/chu kỳ: **tổng 4,77 mW** — tổ hợp 3,13 mW (65,7 %), tuần tự 1,64 mW (34,3 %); internal 2,66 mW (55,8 %) / switching 2,11 mW (44,2 %); **rò rỉ 0,03 µW**, không đáng kể ở 130 nm. Hoạt độ đo từ VCD của mô phỏng **73 lớp thật bằng trọng số thật**: 96.940 chu kỳ, 36,1 M lần đổi bit trên 1.764 bit có đổi. **Bốn giới hạn**: (a) chỉ `ecg_seq`, không phải cả `ecg_coproc`; (b) **không gồm công suất SRAM** — SRAM là blackbox không có model công suất, và nó là 95 % diện tích nên rất có thể chi phối; (c) hoạt độ là **trung bình áp đều**, không phải bản đồ theo net (Verilator tối ưu nhiều tín hiệu thành biến C++ nên VCD chỉ có 184 tín hiệu RTL còn lại); (d) sau global route, không phải post-route — **số cùng giai đoạn với timing (sau DRT+SPEF) là 4,511 mW**, thấp hơn 5,4 %, ảnh hưởng mọi tổng dưới 1 % (`pnr-pdn-full-20260902T003734Z.log`); bài báo nay **nêu giai đoạn** chứ không thay số, vì 4,77 nuôi ~13 chỗ và một bảng 3 hàng. ✅ **Công suất của TOÀN `ecg_coproc` (38,43 mW sau DRT ở 36 ns) ĐÃ GIẢI — nhưng nó là con số của một FLOORPLAN, không của thiết kế, nên bài báo không dùng.** Phạm vi chính xác: nó **gồm rò rỉ SRAM 113,614 µW** nhưng **không gồm công suất ĐỘNG của SRAM** (hàng Macro internal/switching = 0) — nói "loại SRAM" là chưa đủ. ⛔ **Cộng nó với số SRAM của P30 sai HAI LẦN, và lần thứ hai to hơn lần đầu 100×:** (i) rò rỉ đếm hai lần, bớt 0,1136 mW; (ii) **LỆCH TẦN SỐ** — `asic-power-sram.csv` đo ở **40,8 MHz** còn 38,43 mW ở **27,78 MHz** (36 ns), nên động của SRAM 42,346 mW @40,8 phải quy về **28,830 mW @27,78**. Cộng thẳng cho 80,890 mW; **tổng đúng ở 27,78 MHz là 38,4301 + 28,830 = 67,260 mW** (tôi kiểm lại được 67,259). Và bảng đó vẫn giả định `wr_duty = 1,0` trong khi duty ghi **đo được là 12,27 %** — việc của P30, chưa đóng. Nên **chưa có con số "công suất toàn chip" nào dùng được**, và lý do không phải thiếu phép đo mà là **hai phép đo ở hai tần số**. **Hai lần cùng một cơ chế: `report_power` xếp SAI NHÓM.** (a) `31,4 mW` "tổ hợp internal" là **cây đồng hồ** — `clkbuf_*` là cell *tổ hợp* trong liberty sky130hd; phân định bằng một biến duy nhất: giảm hoạt độ **100×** chỉ bớt **4,8 %**, nên 95 % nằm trên net mà `-global` không chi phối. Đo ở hoạt độ 0: sàn 2,191 so với 33,263 mW (15,18×), phần theo dữ liệu 2,320 so với 5,167 (2,23×) → chuẩn hoá **3,234 so với 4,598 nW/cell/MHz = 0,70×, tức logic coproc RẺ hơn**; sàn = 8,40× số `clkbuf` × 2,65× năng lượng mỗi cái, và 2,65 đến từ die lớn **28,75×**. (b) `113,68 µW` "rò rỉ tổ hợp" là **rò rỉ sáu macro SRAM cộng rò rỉ cell chuẩn**: lib cell chuẩn khai `leakage_power_unit : "1nW"` với 0,002–0,012 nW/cell, nên `ecg_seq` dự kiến **~62 nW** so với **30,4 nW** đã báo (cùng bậc, **hợp lý** — nghi vấn "thấp khó tin" bị bác), còn coproc lệch **395×**. ⚠️ **Liberty của MACRO dùng đơn vị `1mW`, lệch 10⁶ so với lib cell chuẩn** — bẫy đơn vị cho ai cộng tay. Đọc đủ chữ số: `39,892 + 60,234 + 13,488 = **113,614 µW**` (macro) `+ 0,066` (cell chuẩn tổ hợp) `= 113,680` khớp hàng Tổ hợp, `+ 0,013954` (flop) `= 113,693954` khớp hàng Tổng **113,694**. ⛔ **Phép khép đầu của tôi ở đây SAI:** tôi cộng cột CSV in 3–4 chữ số ra 113,680 rồi gọi đó là "trùng 5 chữ số, không phải trùng hợp" — phép làm tròn thổi tổng lên **đúng +0,066 µW**, tình cờ bằng chính rò rỉ cell chuẩn, nên câu đó **ngầm khẳng định cell chuẩn rò rỉ bằng 0**. Phần dư 0,066/57.515 = **1,148 pW/instance** so với **1,939** của `ecg_seq`, cùng bậc — nó là **phép đo**, không phải nhiễu. **Bài học:** một phép khép số dùng giá trị **đã làm tròn để hiển thị** không phải bằng chứng dù khớp nhiều chữ số, và **một phần dư nhỏ phải được GIẢI THÍCH**. ⛔ **Giả thuyết max_slew của tôi bị bác HAI LẦN, cả hai đều phân định:** (i) `ecg_seq` có **444** vi phạm — *nhiều hơn* 396 — mà internal/cell thấp hơn 21×, tức **sai chiều**; (ii) chân vi phạm của coproc là chân **dữ liệu** `dout1[*]` còn 29,9 mW nằm trên net **đồng hồ**. **Bài học:** khi một nhóm trong `report_power` lệch một bậc, **kiểm nhãn nhóm trước khi kiểm thiết kế** — nhãn của công cụ là một giả định, không phải phép đo. Số cũ trong ô này: chuẩn hoá theo nhóm thì phần **tuần tự khớp 0,97×** (56,7 so với 55,1 nW/flop·MHz — một phép đối chiếu ĐẠT, xác nhận cùng thư viện/đồng hồ/hoạt độ) nhưng **internal của phần tổ hợp lệch 21,2×** (switching chỉ 2,3×). Giả thuyết "vi phạm max_slew làm internal nở" **bị bác theo CHIỀU NGƯỢC**: `ecg_seq` có **444** vi phạm — nhiều hơn 396 của coproc — mà internal/cell lại thấp hơn 21×. Dây trên mỗi cell 97,6 so với 29,0 µm (3,4×) giải thích một phần, không hết. Chưa giải → không công bố | `make sim-seq-vcd` → `python3 tools/vcd_activity.py` → `ECG_ACTIVITY=... tools/asic-flow.sh pnr` |
| C45 | **Luồng TỪ CHỐI báo công suất khi không có hoạt độ đo được** | — | ✅ Nếu `ECG_ACTIVITY` không được đặt, `asic-pnr.tcl` in một dòng nói rõ nó **bỏ qua** phần công suất, thay vì gọi `report_power` với hoạt độ mặc định. Một con số công suất dựa trên hoạt độ mặc định không phải một phép đo — nó là một phép nhân | `tools/asic-pnr.tcl` |
| C43 | **Ba lỗi trong chính cơ chế báo cáo, và cả ba đều im lặng** | tin vào tệp `*.timing.rpt` | ❌→✅ (a) `report_checks` **in ra stdout và không trả về giá trị**, nên `puts $fh [report_checks ...]` ghi **chuỗi rỗng** — mọi tệp `.timing.rpt` chỉ có tiêu đề, và phát hiện ra khi cần đọc đường tới hạn. Số slack vẫn đúng vì lấy từ stdout. (b) `sta::with_output_to_variable` **cũng** trả về rỗng: chính proc đó làm `set var` trên biến **cục bộ** thay vì `upvar`. (c) `report_checks` không có `-group_count` in đường xấu nhất của **nhóm đầu tiên**, và ở đây nhóm đó là `**async_default**` (recovery trên `rst_ni`) với slack **+22,85 ns** — một đường không ràng buộc gì. Đọc nó như "đường tới hạn" là sai hoàn toàn. Nay in thẳng ra stdout với dấu mốc, và dùng `-group_count 8 -sort_by_slack` | log `pnr-seq-baocao2-*` |
| C42 | **Khoảng quét quá hẹp làm một khối biến mất khỏi bảng** | `ecg_requant` không có hàng dù bước báo xong | ⚠️→✅ Quét trong [2, 20] ns và requant cần 23,59 ns, nên nó không đóng ở đâu và script chỉ in một dòng "không đóng" rồi để trống hàng. Hậu quả: khối **nằm trong** `ecg_seq` và rất có thể là đường tới hạn của cả `ecg_seq` **biến mất khỏi bảng mà không ai biết vì sao**. Nay khi không đóng, script báo **ngoại suy** từ slack ở chu kỳ lớn nhất đã thử (chu kỳ cần ≈ HI + \|slack\|) và ghi hàng `NGOAI-SUY-*` | `tools/asic-flow.sh quet-tan-so` |
| C39 | **Thất bại im lặng của bộ cấp phát sinh blob SAI — đã bịt** | cảnh báo trên màn hình, blob vẫn xuất tên thường | ✅ Khi cấp phát thất bại, `byte_base = 0` dự phòng trỏ vào **vùng đầu vào** (ECG 0..255, RR 256..258), nên blob làm bản ghi 7 ghi 960 B đè lên đầu vào. **Vòng tròn bit-exact của `make isa` vẫn báo BIT-EXACT cả 4 họ** — nó không thấy vì trình thông dịch nhận đầu vào như *đối số hàm* chứ không đọc từ bộ đệm (khoảng trống ADR-0014). Chỉ `tb_ecg_coproc` bắt được: 250/250 nhịp m4, luôn lớp 10, luôn 27/5189 địa chỉ, mẫu **lệch vị trí**. Nay `write_blob` đặt tên `<họ>.INVALID-buf_base.bin` **và báo lỗi** | `make isa` |
| C30 | **E3-5 quyết 8 PE hay 4 PE** — chờ số từ lâu | chỉ biện minh theo diện tích LUT | ✅ **GIỮ 8 PE.** `ecg_mac8` = 31.380 µm² = **1,27 % tổng**. Giảm còn 4 PE tiết kiệm nhiều nhất ~0,7 % diện tích và trả bằng ~2× chu kỳ MAC. Không đáng. Quyết định này trước đây chờ số công suất; số **diện tích** đã đủ để chốt, vì phần bị cắt nhỏ đến mức công suất không thể đảo được kết luận | `make asic-sram`, `asic-area.csv` |
| C31 | **C12 và C21 quyết theo µm²** | hoãn với lý do sai ("FPGA còn dư") | ✅ **BÁC BỎ CẢ HAI.** C12 (ghép hai phép nhân int8 vào một DSP48E1) **không có nghĩa trên ASIC** — không có DSP48E1 nào. C21 (dùng chung một `ecg_requant`) tiết kiệm nhiều nhất 13.377 µm² = **0,54 % tổng**, đổi bằng một bộ phân xử và một đường trễ. Không đáng. Dự đoán ở E3-8 rằng C12 sẽ bị bác bỏ **đã đúng** | `asic-area.csv` |
| C32 | **Tối ưu thật sự: thu `ecg_actbuf` 6.144 → 4.448 B** | ADR-0015 chọn 6.144 B vì trên FPGA 3 × RAMB18 là *miễn phí* | 🔜 **Tiết kiệm 237.492 µm² = 9,6 % tổng = 2,01 lần TOÀN BỘ phần logic.** Đỉnh dùng đo được là 4.419 B (`ECG_PEAK_ACT_BYTES`); granularity macro buộc cấp 4.608 B (`128x256` + `80x64`). Đây là hạng mục tối ưu **lớn nhất còn lại của cả dự án**, và nó lớn hơn tổng mọi tối ưu mức LUT đã làm cộng lại. Cái "miễn phí" trên FPGA tốn 0,31 mm² trên ASIC | `make asic-sram` |
| C33 | **Kiểm thử tiêu cực: cố ý làm mỗi assertion bắn** | 12/20 kịch bản tiêu cực **chưa có kích thích nào** — chưa biết các `$error` đó có bắn được không | ✅ Kiểm kê **44 `$error` + 34 `assert`** trên 13 tệp. Trong 21 `$error` của ba tệp mục tiêu: **16 kích thích được**, **5 rỗng theo cấu trúc**. Ba ca đối chứng hợp lệ bắn 0 lần; biên `rd_off_i = BYTES−8` bắn 0 và `BYTES−1` bắn 1 → biên đặt đúng chỗ. Không có assertion nào vi-phạm-đúng-mà-im | `make sim-negative` |
| C34 | **Phát hiện về PHẠM VI: không một `assert` nào tồn tại trong mô phỏng** | "34 assert" | ⚠️ **Cả 34 `assert` đều nằm trong `` `ifdef FORMAL ``**, và `VFLAGS` không định nghĩa `FORMAL`. Trong mô phỏng Verilator chỉ có `$error` là sống. Điều này không sai — assert được viết cho formal — nhưng con số "34 assert" không được đọc như độ phủ mô phỏng | đọc mã |
| C35 | **Một thuộc tính formal trong "27 thuộc tính" là RỖNG — đã thay bằng thuộc tính thật** | `cvxif` #1: `assert(!(push && full))` | ❌→✅ `push` được **gán** bằng `iss_valid_i && is_compute && !full`, nên `push && full` là **hằng sai** và assertion là hằng đúng. Nó **không** chứng minh FIFO không tràn; một lỗi chỉ số bit trong `full` sẽ không làm nó bắn. Thay bằng chặn **chiếm dụng**: `(wptr_q − rptr_q) ≤ FIFO_DEPTH`, cộng `!(empty && full)`. **PASS**, và **đột biến hạ chặn xuống FIFO_DEPTH−1 bắn ở bước 7** → chặn là chặt, FIFO thật sự đạt chiếm dụng bằng độ sâu | `make formal`, `make formal-mutant` |
| C36 | **Hai thuộc tính `cvxif` còn lại là PHÁT BIỂU LẠI phép gán** | tính là thuộc tính độc lập | ⚠️ #2 (`iss_ready_o` ba nhánh) và #3 (`iss_accept_o == in_space`) so một tín hiệu với **chính công thức gán nó**, nên không thể bắn với RTL hiện tại. Giá trị của chúng là giá trị của một phép kiểm **hồi quy** — nếu ai đó nới điều kiện thì chúng bắn. Đó là mục đích thật nhưng **yếu hơn nhiều** một thuộc tính về trạng thái. Đã ghi nhãn trong mã; **con số "27 thuộc tính formal" phải được đọc với hiểu biết đó** | đọc mã |
| C37 | **`ecg_coproc.sv` §483 nói kiểm chéo hai đường suy nhưng chỉ có MỘT dây** | tính là kiểm chéo | ⚠️ `d_relu` **chính là** `relu_o` của `u_desc`, được gán bằng `(act == ECG_ACT_RELU)` — đúng biểu thức mà assertion so nó với, cùng một instance. Cần hoặc một đường suy thứ hai thật, hoặc bỏ tuyên bố kiểm chéo | đọc mã |
| C38 | **Sai lệch trong chính đặc tả kiểm thử** | `DAC-TA-KIEM-THU.md` §6 ghi TC-NEG-018 ✅ | ❌ **Sai.** `cvxif_ref.py` gọi `encode()` với `resv` mặc định 0 ở mọi lời gọi; không bản ghi nào trong `vectors/cvxif.txt` đặt bit 26:25. Kịch bản đó **trống**; nay mới có kích thích thật. TC-NEG-014/015/016 đúng cho testbench nhưng **quy công sai** cho ba `$error` hằng sai | `make sim-negative` |
| D0 | **Benchmark cả bốn họ vs ba đối thủ** (trước chỉ có m2) | inter: m3 0,6299 · m2 0,6256 · m4 0,6109 · m1 0,5982 · **wideconv 0,6023** · lee2025 0,5626 · morphonly 0,4503. within: **wideconv 0,8637** · m2 0,8501 · m1 0,8498 · m4 0,8445 · m3 0,8413 · lee2025 0,8332 · morphonly 0,7879 | — | ⚠️ **inter: 3/4 vượt đối thủ mạnh nhất** (m3 +2,76 · m2 +2,33 · m4 +0,86), m1 **thua 0,41**. **within: 0/4 vượt** — wideconv thắng 1,36–2,24 điểm với **ít tham số hơn cả bốn**. Khoảng cách within không mạnh về thống kê (σ wideconv = 1,52 điểm) nhưng chiều rõ. **Thứ hạng ĐẢO**: m3 tốt nhất ở inter, kém nhất ở within → bằng chứng mạnh nhất cho ADR-0011 | `make benchmark` |
| D1 | **Chi phí chuyển mô hình đo được** | T_switch 667 chu kỳ = **96 điều khiển + 571 DMA** | **0/22 công trình đo** | ⚠️ mô hình chu kỳ, chưa đo board. **Phải nêu kèm phân rã (E3-6):** 86 % là chuyển byte, nên luận điểm đúng là “băng thông DMA đủ + cơ chế khéo cho phần còn lại”, không phải “cơ chế chuyển mô hình khéo”. 571 chu kỳ DMA là hệ quả của chính sách thường trú *một* bộ trọng số — chính là cách giải xung đột N7 ↔ N3 |
| D2 | ≥ 4 mô hình cùng tác vụ cùng tín hiệu | 4 | tất cả = 1 | ⚠️ chưa xác nhận tổng hợp |
| D3 | Ràng buộc phần cứng là bộ chính quy hoá | N1 = **−4,01** (ResNet) | không ai báo | ✅ DS2 lần 2 |
| D4 | Hiệu ứng giao thức áp đảo kiến trúc | 22,5–33,8 so với 3,1–6,3 điểm | không ai đo | ✅ `make benchmark` |
| D5 | Giao thức **đảo thứ tự xếp hạng** | within: wide-conv thắng; inter: ta thắng | không ai đo | ✅ |
| D6 | Đạo trình > tần số lấy mẫu > số mẫu | `edb` +4,6; `incartdb` −1,1 | không ai đo | ✅ |
| D7 | Chính sách lượng tử hoá theo họ | `CONCAT` đòi ngược 3 họ kia | không ai báo | ✅ |
| D8 | **Đường fake-quant lệch phần cứng 10,5 điểm** | 10,47 → **1,12** sau sửa (bản ràng buộc, Inception; bản tự do 2,44) | không ai báo | ✅ `python3 tools/hw_gap.py`. Con số 1,76 trong bản trước **đã lỗi thời**: `hw-gap.csv` sinh ở commit `9755852`, trước khi ADR-0014 §2.1 bỏ một lần làm tròn kép ở nhánh concat — chỉ Inception có CONCAT nên đúng nó dịch +0,64 điểm. **Và đường phần cứng không đảo dấu kết luận N1 của họ nào trên validation** (CNN +1,34 → −0,13 · ResNet +2,04 → +2,57 · MobileNet −6,33 → −7,08 · Inception +1,35 → **+0,03**) |
| D9 | Bơm 0 thay xoá bộ đệm | **864 → 96 chu kỳ** (đo lại ở bộ đệm 6.144 B), 12,1 % lần đọc là biên | không ai báo | ⚠️ như D1 |
| D10 | Năng lượng mỗi lần suy luận | chưa có số | 0,06–10 µJ | ❌ cần P7 |

**Bảy đóng góp đã có số thật.** D1 và D9 là hai đóng góp trung tâm của đề tài và **cả hai vẫn là
mô hình chu kỳ** — chúng chỉ thành đóng góp thật khi đo trên board ở P7.

---

## E. Cửa để qua bước tiếp theo

Mỗi bước chỉ được coi là xong khi **cả bốn** điều kiện đúng:

1. `make lint-rtl` sạch, **không cảnh báo**, `-Wall`
2. Testbench của module mới đối chiếu **một hiện thực riêng** của cùng đặc tả
3. `make fpga-res` có số, và số đó được ghi vào bảng §C
4. `make sim-leaf` vẫn đạt toàn bộ — không hồi quy

Điều 2 là điều dễ bỏ qua nhất và quan trọng nhất: nếu vector kiểm sinh từ chính RTL thì lỗi chung
sẽ triệt tiêu và test đậu oan. Trong P5 đã có **năm lần** harness sai mà RTL đúng; mỗi lần đều
phát hiện được nhờ một con số vô lý (31.374 lỗi trên 22.718 vector; 8 bộ đệm thay vì 4).

## E'. Ba bộ phép thử MỚI — mức CẦU, mức NGOẠI VI, và mức HỆ

Ba bộ này lấp một khoảng mà `grep -c "src/soc" Makefile` = **0** đã phơi ra: hai tệp
`ecg_core_xif.sv` và `ecg_xif_bridge.sv` **không nằm trong bất kỳ phạm vi nào** — không
lint, không sim, không formal. Lỗi không "lọt qua" phép kiểm; nó sống ở chỗ **không có
phép kiểm nào**.

### C31 · `make sim-xif-bridge` — cầu CV-X-IF, 9 phép thử

| phép thử | điều khoản |
|---|---|
| T1 | một lệnh không chặn → **đúng 1** bắt tay |
| T2 | skid đầy → `iss_valid_o` giữ 0, và sau khi rút → **đúng 1** bắt tay |
| T3 | `id`/`rd`/`data` của kết quả khớp lệnh đã bắt tay |
| T4 | `result_valid` giữ cho tới `result_ready` |
| **T5a** | huỷ một lệnh **đã nhận** (id khớp) → cờ `kill_seen_o` bật, và **dính** |
| **T5b** | huỷ một id **chưa hề phát** → cờ giữ **0** |
| T6 | commit không kill → cờ giữ 0 |

**T5b là nửa phân định, và bản trước không có nó.** Bản cũ gửi `commit_kill` mà không
phát lệnh nào rồi đợi cờ bật — nó **đạt vì một lỗi**: điều kiện cũ chốt *mọi*
`commit_kill`, và CV32E40X gửi commit cho **mọi** lệnh nên một phép huỷ của một lệnh
**thường** (xả pipeline lúc boot) cũng làm cờ bật. Đo được: một firmware tối thiểu
**không có một lệnh custom nào** vẫn làm cờ bật sau **37 chu kỳ**.

### C32 · `make sim-mmio` — ngoại vi ánh xạ bộ nhớ, 15 nhóm

Hai điều khoản chịu lực lấy từ **chính** `tb_ecg_coproc.cpp` chứ không từ suy diễn:
đọc descriptor và đọc scale/bias là **tổ hợp** (dòng 180-186 và 190-200). T3 kiểm bằng
cách đặt `desc_req` rồi đọc **mà không tick**, rồi đổi chỉ số và đòi dữ liệu đổi **ngay**.

| nhóm đáng ghi | điều khoản |
|---|---|
| T5 | bias là **9 bit CÓ DẤU**: ca thử là **−170**, và nếu cài đặt ép về int8 thì đọc ra **+86** — phép thử in ra chính dấu hiệu đó |
| T7 | chỉ thu kết quả **lớp cuối**: 1 ghi giữa + 1 ghi cuối → `RESCNT = 1` |
| T14 | OBI đòi `rvalid` cho **cả phép ghi** — hồi quy cho việc lõi đứng vĩnh viễn ở `sw` đầu tiên |
| **T15** | **đẩy VÀ rút cùng chu kỳ** → số đếm FIFO phải **giữ nguyên** |
| T11 | cờ chỉ số ngoài bảng phải bằng **0** với tham số mặc định — nếu chúng lên thì đó là lỗi cắt bit `6'(64) = 0` |

### C33 · `make sim-soc` — mức HỆ, suy luận thật bit-exact

Firmware thật trên CV32E40X nạp mô hình từ DMEM (9 descriptor · 4.576 B trọng số · 106
mức thang · 105 mức bias · 256 mẫu + 3 đặc trưng RR), chạy 9 lớp qua **lệnh custom**, và
đọc 5 điểm lớp: **`23 −11 −17 −6 −76`**, khớp **từng bit** tham chiếu Python, ở **cả hai**
chế độ (một-lớp qua ISA, và tự đi qua MMIO).

**Ba lỗi mà phép thử mức khối không thể thấy**, và lỗi thứ ba là lập luận mạnh nhất cho
việc phải có mức hệ:

| lỗi | vì sao mức khối không thấy được |
|---|---|
| OBI không trả `rvalid` cho phép ghi | không có lõi thật thì không ai chờ `rvalid` |
| số đếm FIFO giảm khi đẩy/rút trùng chu kỳ | C++ lái trực tiếp `ws_valid`/`ws_ready`, không qua FIFO |
| **ngăn xếp đè ảnh trọng số** | không có firmware thì không có ngăn xếp. Và nó chỉ hiện ra thành điểm lớp lệch **nhỏ**, lệch **khác nhau** mỗi lần sửa firmware, **trong khi phép đọc lại bảng vẫn báo 0 sai** |

Ba cách thất bại được **phân biệt**, không gộp thành "không chạy": `SIG(0) = 0x1EC6A11C`
là `main()` trả về bình thường · `0x7BADC0DE` là **TRAP** kèm `mcause`/`mepc` · `0` là hết
giờ. `mtvec` được đặt *trong* `crt0` đúng vì lý do đó — mặc định `mtvec = 0` làm một lệnh
không hợp lệ nhảy vào IMEM chưa nạp và **treo**, tức triệu chứng xa hoàn toàn nguyên nhân.

## F. Thứ tự còn lại của P5

| Bước | Hạng mục | Mở khoá |
|---|---|---|
| ~~P5.2a~~ | ~~B13 `CONCAT` theo `dst_off`~~ | ✅ xong: 4/4 bit-exact, N7 12,92 kB, N3 667 chu kỳ (96 điều khiển + 571 DMA) |
| ~~P5.2b~~ | ~~B14 `ecg_wmem` + DMA~~ | ✅ xong: 4/4 cổng qua, 285 LUT / 2 RAMB |
| ~~P5.2c~~ | ~~B18–B20 `MAXPOOL`, `GAP`, `ADD`~~ | ✅ xong: 4/4 cổng qua, 124 LUT, phủ hết opcode tính toán |
| ~~P5.3a~~ | ~~B15 giải mã descriptor~~ | ✅ xong: 4/4 cổng qua, 16 LUT |
| ~~P5.3b-0~~ | ~~C16 chuyển vị bố cục trọng số~~ | ✅ xong: `make wlayout` 2.577/2.577 phép đọc đúng, SRAM trọng số 4.664 B |
| ~~P5.3b-1~~ | ~~C17 điền trường hình dạng vào descriptor~~ | ✅ xong: `make selfcheck` 102/102, 0/44 bản ghi thiếu |
| ~~P5.3b-2~~ | ~~B16 sequencer, đường MAC~~ | ✅ xong: 68 lớp bit-exact, 1.171 LUT, 0 DSP thêm |
| ~~P5.3b-3~~ | ~~B16 sequencer, đường MAXPOOL/GAP/ADD~~ | ✅ xong: 42 lớp bit-exact, 619 LUT, phủ hết opcode tính toán |
| ~~P5.3b~~ | ~~Ghép cả bộ đồng xử lý, chạy hết một mô hình~~ | ✅ **N2 ĐẠT**: 1000/1000 nhịp, 2.455 LUT |
| P5.3c | B17 shim CV-X-IF | tổng hợp cả khối |

## Bất biến 49 · một con số là HÀM của một lựa chọn tuỳ ý, trình bày như một phép đo

> **Số hiệu:** ban đầu tôi ghi "52". Số cao nhất đang dùng trong kho là **48**
> (grep trên `.md`/`.py`/`.sh`/`.cpp`/`.sv`/`.h`/`.html`), và 49–51 chưa ai dùng.
> Nên "52" là một con số tôi **phát biểu mà không đo** — trong chính mục nói về
> việc phát biểu con số không đo. Đã sửa thành 49.

**Ba lần trong một ngày (2026-09-04), ba tệp, một hình dạng.** Cả ba đều là chỗ
một người viết một con số mà **không nói nó phụ thuộc cái gì** — và cả ba con số
đều *đo được*, không cái nào bịa.

| con số | là hàm của | trình bày như | phép sửa |
|---|---|---|---|
| `2{,}482` Slice LUT | ràng buộc thời gian (2468 ở 17,0 ns → 2482 ở 15,923) | một thuộc tính của thiết kế | thành **khoảng** `2468--2482` kèm hai đầu ràng buộc |
| `2.9` và `2.3` điểm | luật làm tròn (nửa-lên cho 2,9; `%.1f` cho 2,8) | một giá trị tính được | thành `2.85` / `2.25` — bỏ hẳn lớp "hai độ chính xác" |
| `2nd` / `3rd` thứ hạng | phép phá thế hoà (m1 = m2 = `0.8449` within) | một thứ hạng đo được | thành *"m1 and m2 hold the two ends of inter-patient and tie within-record"* |

**Phép thử để bắt nó:** với mỗi con số trong bài, hỏi *"nếu tôi đổi một lựa chọn
mà tôi không coi là dữ liệu — ràng buộc, luật làm tròn, thứ tự sắp — thì con số
này có đổi không?"* Nếu có, thì lựa chọn ấy là **phần của con số** và phải in ra
cùng nó, hoặc con số phải thành một khoảng.

**Vì sao nó khó bắt hơn một con số bịa:** một con số bịa không tái lập được, nên
cổng truy nguyên bắt được. Một con số kiểu này **tái lập hoàn hảo** — chạy lại
đúng cùng lựa chọn thì ra đúng cùng giá trị. Nó chỉ sai khi ai đó đọc nó như một
thuộc tính, và đó là điều một cổng không thấy.

**Họ hàng gần:** bất biến "đơn vị và CẤU HÌNH là phần của con số". Bất biến này
là dạng mạnh hơn: không chỉ đơn vị và cấu hình, mà **mọi lựa chọn không phải dữ
liệu**.

## Bất biến 50 · một phép kiểm không chứng minh được rằng CHỦ THỂ của nó đã thay đổi

**Bốn lần trong một ngày (2026-09-04), bốn cơ chế, một hình dạng:** một phép kiểm
chạy trên một chủ thể **không đổi** và báo kết quả **như thể** nó đã đổi.

| ca | cơ chế | báo gì | thật ra |
|---|---|---|---|
| ba đột biến `sed` | khuôn `sed` không khớp (dòng không bọc nháy) | *"cổng không bắt"* | tệp **không đổi**, cổng đúng khi báo đạt |
| `$?` sau ống dẫn | `\| tail -6; echo $?` | `rc=0` | mã là của `tail`; công cụ trả **1** |
| cổng xanh vì tiêu đề cũ | `assert` ném **trước** khi ghi tệp | `59/59 đạt` · `khai 0 dòng` | dữ liệu có cột mới, **tiêu đề** vẫn cột cũ |
| phép so băm sau refactor | script hỏng (`NameError`) nên không ghi bảng | `GIỐNG-TỪNG-BYTE` | băm giống vì **không có gì xảy ra** |

**Khác bất biến 49 ở đâu:** 49 nói về một *con số* là hàm của một lựa chọn không
nói ra. Cái này nói về một *phép kiểm* mà chủ thể của nó không hề bị chạm — nên
kết quả "khớp"/"không bắt" là một phát biểu về **hư không**.

**Hai chốt chung, cả hai rẻ:**
1. **Mọi phép thử phá-để-kiểm phải khẳng định tệp ĐÃ ĐỔI** trước khi đọc kết quả
   — `cmp -s` trước và sau, và một **chốt âm** (một `sed` cố ý không khớp) để
   chính chốt đó được chứng minh.
2. **Mọi phép so băm phải khẳng định bước SINH đã thành công** (mã trả về) trước
   khi so. Một băm giống nhau sau một bước sinh thất bại là băm của tệp cũ.

**Và một chốt thứ ba cho lớp `$?`:** dòng cuối của một công cụ phải nói kết luận
**bằng chữ**, không chỉ trả một mã — để một mã trả về bị ống dẫn ăn mất vẫn còn
một đường nói. Đó là cái đã cứu ca thứ hai: `manifest KHÔNG khớp` là câu duy nhất
còn lại sau khi `tail` ăn mất mã `1`.

### Nửa thứ hai: khoảng trống giữa phép kiểm CHẠY và câu khai DỰA TRÊN nó

Bốn ca trên là *chủ thể không đổi*. Có một nửa thứ hai, và cả hai session đều mắc
nó trong cùng một ngày:

| ca | lệnh | khai gì | thật ra |
|---|---|---|---|
| `afd2aed` (session này) | `make checks 2>&1 \| tail -1` rồi `git commit` **cùng một lệnh** | *"17 cổng, tất cả đạt"* | `make checks` **đỏ**; dòng `CO PHEP KIEM HONG` hiện ra sau khi commit đã tạo |
| `ef00631` (session song song) | y hệt | *"make checks: 18/18"* | y hệt |

Ở đây phép kiểm **có** chạy và chủ thể **có** đổi — nhưng câu khai được viết
**trước khi kết quả được đọc**. Gộp `make checks` và `git commit` vào một lệnh
tạo ra đúng khoảng trống đó: shell chạy tuần tự, nhưng *con người* viết commit
message trước cả hai.

**Chốt:** đọc kết quả trong **một lệnh riêng**, rồi mới viết câu khai. Hoặc: đừng
khai một con số về trạng thái cổng trong commit message — dẫn lệnh để người đọc
tự chạy.

**Ba mảnh của luật in, đầy đủ:**
- in **giá trị trung gian** — một giá trị vô lý (`sàn cấp phát 234608 B`) đập vào mặt
- in **giá trị KỲ VỌNG** — `0` hợp lệ về kiểu, chỉ đáng ngờ nếu biết phải là 8
- in **kết luận bằng chữ** — còn nói được khi mã trả về bị ống dẫn ăn

*Cả bốn ca do phiên chạy song song tìm ra và tự kể, ba trong bốn là lỗi của chính
họ trong lúc đang dọn đúng lớp lỗi đó.*

---

## Bất biến 51 · một dấu hiệu có thể **đúng** mà nói về một đại lượng KHÁC

**Ba lần trong nửa tiếng (2026-09-04), ba cơ chế, một hình dạng:** một dấu hiệu
được đọc **đúng như nó là**, rồi được dùng để trả lời một câu hỏi mà nó không nói
gì về câu hỏi đó.

| ca | dấu hiệu | câu hỏi đang hỏi | dấu hiệu thật ra nói về |
|---|---|---|---|
| `rc` **của ai** | `rc=0` từ một lệnh vừa đặt `run_in_background` **vừa** viết `nohup … &` | *"`make coverage` có chạy xong?"* | mã ra của **lớp bọc**; `make` vẫn chạy dưới nền và xong 14/14 sau 437 s |
| | `rc=2` của một đột biến RTL | *"phép thử có bắt được không?"* | `%Error-SELRANGE` — `id_pend_q[4:0]` trên một số **4 bit**, tức không **dựng** được |
| | `rc=1` của một phép thử chốt | *"chốt có bắn không?"* | `sed` phân cách bằng `\|` trong khi khuôn có `\|` → script chạy **bản chưa sửa**; lần sau: bản sao sai độ sâu nên `cd "$(dirname $0)/../.."` trỏ sai gốc |
| **mtime** thay nội dung | `.dat` cũ hơn nguồn | *"con số này là của bản nguồn hiện tại?"* | **thời điểm ghi tệp** — vòng thử đột biến `cp` bản gốc trở lại **sau** khi đo, nên nguồn mới hơn trong khi **nội dung** đúng bằng cái đã đo → **báo oan** |
| log **đang ghi** | log dứt giữa dòng ở `sim-requant`, thư mục `.dat` rỗng | *"lượt chạy có bị giết?"* | **chỗ nó vừa tới**; `do_coverage.sh:78` cố ý `rm -f` từng `.dat` trước khi sinh lại |

**Khác bất biến 50 ở đâu:** 50 nói về một phép kiểm mà **chủ thể không hề bị
chạm** — kết quả là phát biểu về hư không. Cái này nói về một dấu hiệu **có thật,
đọc đúng**, nhưng thuộc về một đại lượng khác. Nó khớp với cả hai khả năng đang
phân định, nên theo bất biến 21 nó **không mang tin** — dù nó chính xác.

**Và ca thứ ba đắt hơn hai ca đầu, vì cái giá không phải một báo cáo sai mà một
HÀNH ĐỘNG sai:** tin rằng lượt chạy đã bị giết, tôi phóng **lượt thứ hai chồng
lên lượt đang chạy** — hai `make` cùng đích, cùng `rm -f` một `.dat`, cùng dựng
một nhị phân. *Đó* mới là nguyên nhân `sim-coproc` trượt `rc=2` và bảng
`coverage.csv` bị thu hẹp mất một hàng đã commit. Lỗ trong `do_coverage.sh` là
thật và bản vá là đúng, nhưng nó lộ ra vì tôi **tự gây ra ca thử**.

**Ba chốt, cả ba rẻ:**
1. **Một `rc` phải nói kèm CHỦ THỂ.** Trước khi đọc `rc` như câu trả lời, hỏi
   *"mã này của tiến trình nào?"* — không bao giờ đặt `run_in_background` cùng
   `&`/`nohup`, và với một đột biến thì phải **phân định** `rc` do phép thử hay
   do trình biên dịch (đọc dòng `%Error`).
2. **Một chốt về NỘI DUNG phải đo nội dung.** Chốt tươi-cũ của
   `coverage_phanra.py` băm `sha256` từng tệp nguồn mà `.dat` **tự khai** ở
   trường `f`; mtime bị bỏ vì nó đo thời điểm, không đo nội dung.
3. **Một log chưa có dòng kết thúc là log CHƯA XONG.** `tools/run-logged.sh` ghi
   dòng `# ma tra ve: … · thoi gian: …` ở cuối — không có dòng đó thì lượt chạy
   **đang chạy**, và mọi kết luận về nó là kết luận về một ảnh chụp giữa lượt.
